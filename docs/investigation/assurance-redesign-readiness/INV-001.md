# IIS assurance 재설계 검토·착수 준비 Repository Investigation

Artifact-Type: repository-investigation
Format-Version: 1
Status: complete
Project-Root: /home/user01/project/iis-skills
Repository-Root: /home/user01/project/iis-skills
Investigation-Slug: assurance-redesign-readiness
Investigation-Revision: INV-001
Observed-At: 2026-09-19T10:06:07+09:00
Git-Branch: main
Git-Head: c856dd7257d518b2eba361f289ff6b28a74d731a
Working-Tree-Before-Artifact: clean
Evidence-Authority: repository evidence only; not IIS planning authority

## Evidence Handoff

### Investigation Answer

Answer: 검토·착수 준비는 완료했다. 원문의 Reviewer/Verifier 제거와 실제 관측·반례 중심 전환은 구현 후보로 유지할 수 있지만, 현재 소스 기준 T11 중복 수정 제거와 inspect 경계 보완, T10 누락 참조 추가, 직접 관측 성공 판정 및 dispatch 검증 경계 명시가 필요하다. T01–T17의 변경·보존·검증 대상을 아래에 정리했다. 구현, 정식 Scope/Plan 승인, 행동 평가, 운영 전환은 수행하지 않았다.
Based On: Finding 1, Finding 2, Finding 3, Finding 4, Finding 5, Finding 6, Finding 7, Finding 8, Finding 9, Finding 10, Inference 1, Inference 2

### Observed Current Product State

- State: 저장소 main은 원문 기준 61040d24f1b6e9ea1c265c1c7196df8e67afe2e3보다 한 커밋 앞선 c856dd7257d518b2eba361f289ff6b28a74d731a이며, 조사 시작 시 working tree는 clean이었다.
  Supporting Findings: Finding 1
- State: OMP 설치 bundle은 f41ceb4e8fcb8e29234fdb9c354ce20c03219a667009527c8c63a83e90bc1770, protocol 4, payload 54개다. installer inspect는 성공했고 경로 rebase 후 소스 payload와 설치 payload의 바이트 불일치는 0개였다. loaded_identity는 NOT_CHECKED다.
  Supporting Findings: Finding 4
- State: 독립 Plan Review, Scope Verifier, post-VERIFIED exactly-one Probe는 여전히 현행 계약이다. 새 assurance helper는 조사한 workflow 디렉터리에 아직 없다.
  Supporting Findings: Finding 5, Finding 7

### Existing Product Surfaces And Readbacks

- Trigger: scripts/sync_installed_iis.py inspect --store /home/user01/.local/share/iis
  Observable surface: bundle ID, family, protocol, payload file count, loaded_identity
  Authoritative readback: installed.json, current symlink, release bundle.json 및 실제 파일 바이트·mode·managed links
  Supporting Findings: Finding 2, Finding 3, Finding 4
- Trigger: Scope 완료 기록
  Observable surface: 정확한 SCOPE.md의 Status 한 항목
  Authoritative readback: 현재 scope-verify/SKILL.md:73-85의 원본 currentness, effects settlement, status-only write/readback 절차
  Supporting Findings: Finding 5
- Trigger: Observatory의 ready/done 상태 조회
  Observable surface: next_work 설명과 현재 Scope 상태
  Authoritative readback: 읽은 Scope 및 bound sources; 전체 요청 완료나 실행 권한이 아님
  Supporting Findings: Finding 10

### State And Authority

- Statement: 원문은 새 설계 제안이고 현재 실행 계약의 자동 대체물이 아니다. 이번 사용자 요청은 문서 검토와 작업 준비이며 구현·삭제·commit·push·운영 설치 전환은 포함하지 않았다.
  Supporting Findings: Finding 5
- Statement: Scope와 Thesis의 제품 의미, 승인된 Transition 경계, 현재 구현 actor 권한과 non-idempotent effect settlement는 역할 삭제와 분리해 유지해야 하는 기존 경계다.
  Supporting Findings: Finding 5, Finding 6

### Planning Constraint Candidates

- Candidate: T11에서 이미 수정된 check_install의 자기 manifest 검사를 다시 구현하지 않는다. candidate 적합성 검사와 기존 설치 상태 관측을 구분하고, 새 topology를 쓰는 도구의 rollback 이후 inspect 경로를 별도로 다룬다.
  Supporting Findings: Finding 2, Finding 3
- Candidate: T10에 product-thesis/references/exploration.md:83,97을 포함한다. purpose-first-review/dispatch/reviewer.md는 삭제할 Plan Reviewer와 다른 역할이므로 유지한다.
  Supporting Findings: Finding 6, Finding 7
- Candidate: T01/T02는 직접 관측의 존재와 요구 충족을 구분해야 한다. 실제 dispatch bytes·입력 공개 순서 검증은 host 실행 증거로 확인하며 문서 문자열 테스트를 행동 증명으로 승격하지 않는다.
  Supporting Findings: Finding 7, Finding 9, Inference 1, Inference 2

### Product Dependency Candidates

- Candidate: 설치 전환 전에 하나의 일관된 candidate payload가 필요하다. helper, 역할 문서, dispatch, installer required/retired 목록과 현재 문서를 함께 맞춰야 한다.
  Supporting Findings: Finding 2, Finding 5, Finding 6
- Candidate: LLM 탐지력 비교에는 새 candidate와 실제 독립 invocation, 사전에 정한 model/effort·반복·예산이 필요하다. 현행 fixture/scorer만으로 개선이 입증되지는 않는다.
  Supporting Findings: Finding 8, Finding 9

### Material Unknowns

- None

### Load-Bearing Anchors

- A1 | DOC | /mnt/c/Users/Junny/Downloads/붙여넣은 마크다운(1)(20260918-223444).md | 전체 1178행; 특히 155-161, 325, 487-517, 822-885, 1033-1042 | sha256:2aa8a667abc691759f9f23c3602f5cacdb02e9c588ceeee1cc8e80ceaccb1025 | EXTERNAL_VERSION
- A2 | SOURCE | Git repository /home/user01/project/iis-skills | rev-parse, status --short, log -6, diff 61040d24..HEAD | c856dd7257d518b2eba361f289ff6b28a74d731a | ANCHOR_LOCAL
- A3 | SOURCE | scripts/sync_installed_iis.py | REQUIRED/PAYLOAD_ROOTS; check_release 154-181; check_candidate_release 184-192; check_install 195-209; inspect 211-220; activate 384-463; rollback/remove 466-497 | c856dd7257d518b2eba361f289ff6b28a74d731a | ANCHOR_LOCAL
- A4 | TEST | tests/test_ready_ticket_boundary_tools.py | setUp 27-46; test_installed_release_uses_its_own_manifest_during_upgrade 189-195; rollback/migration assertions 161-175,209-258 | c856dd7257d518b2eba361f289ff6b28a74d731a | ANCHOR_LOCAL
- A5 | RUNTIME | /home/user01/.local/share/iis | CLI inspect; installed.json; bundle.json; source rebase comparison; in-memory future REQUIRED experiment | bundle f41ceb4e8fcb8e29234fdb9c354ce20c03219a667009527c8c63a83e90bc1770; 2026-09-19 | RUNTIME_STATE
- A6 | SOURCE | iis-workflow/SKILL.md; companion-skills/scope-plan/SKILL.md; companion-skills/scope-implement/SKILL.md; companion-skills/scope-verify/SKILL.md | Canonical authority flow; admission; Completed-result fan-in, Probe and recording | c856dd7257d518b2eba361f289ff6b28a74d731a | ANCHOR_LOCAL
- A7 | SOURCE | product-thesis/references/exploration.md | Classify before changing Thesis 74-87; Revise and check connected consequences 89-97 | c856dd7257d518b2eba361f289ff6b28a74d731a | ANCHOR_LOCAL
- A8 | SOURCE | companion-skills/production-heuristic-probing/SKILL.md; companion-skills/production-heuristic-probing/dispatch/probe.md; iis-workflow/references/delegated-role-dispatch.md | normal/standalone invocation; Required; role projection list 148-159 | c856dd7257d518b2eba361f289ff6b28a74d731a | ANCHOR_LOCAL
- A9 | TEST | tests/test_delegated_role_dispatch.py; tests/test_corrective_reuse_contract.py; tests/test_failure_space_completeness.py; tests/test_production_heuristic_probe_contract.py | phrase assertions, packaged projections, causal fixture assertions | c856dd7257d518b2eba361f289ff6b28a74d731a | ANCHOR_LOCAL
- A10 | SOURCE | evaluation/ready-verification/score_result.py | score 56-162 | c856dd7257d518b2eba361f289ff6b28a74d731a | ANCHOR_LOCAL
- A11 | DOC | evaluation/ready-verification/README.md; evaluation/ready-verification/delegated-dispatch-scenarios.md; evaluation/ready-verification/corrective-reuse-scenarios.md | current tool boundary 1-33; Status NOT_RUN; D01-D04; E1-E8 | c856dd7257d518b2eba361f289ff6b28a74d731a | ANCHOR_LOCAL
- A12 | TEST | tests/test_ready_verification_fixture_boundaries.py | wrong-entry 36-47; shared parser defect/normal twin 50-80; external-effect fixture 82-113 | c856dd7257d518b2eba361f289ff6b28a74d731a | ANCHOR_LOCAL
- A13 | SOURCE | observatory/src/iis_observatory/next_work.py; scripts/sync_installed_observatory.py | ready/done 57-89; INSTALLED_DIR and FILES 12-15 | c856dd7257d518b2eba361f289ff6b28a74d731a | ANCHOR_LOCAL
- A14 | SEARCH | iis-workflow/**; product-thesis/references/**; product-thesis/dispatch/**; current docs; companion role paths named in T01-T17 | bounded glob and grep for assurance, scope-verify, Plan Review, ADMIT, reviewer | c856dd7257d518b2eba361f289ff6b28a74d731a | SEARCH_UNIVERSE
- A15 | SOURCE | companion-skills/scope-plan/references/plan.md; companion-skills/scope-implement/references/implement.md | Code Grounding; Conditional first work; Revision ownership; correction reproducer and execution handoff | c856dd7257d518b2eba361f289ff6b28a74d731a | ANCHOR_LOCAL

## Investigation Charter

### Original Request

"C:\Users\Junny\Downloads\붙여넣은 마크다운(1)(20260918-223444).md" 검토하고 실제 작업 준비 해

### Investigation Objective

원문의 현행 사실과 T01–T17 작업 목록이 현재 로컬 IIS 소스·설치와 맞는지, 실제 착수 시 중복·누락·계약 불일치를 일으킬 경로가 무엇인지, 다음 구현에 필요한 근거와 경계를 정리한다.

### Decision Context

Reviewer/Verifier 삭제를 평가 없이 승인하거나 억제하는 것이 아니라, 기존 소스를 잘못 전제로 삼는 수정과 이름만 바꾼 심사 재도입을 피하고 실제 관측·반례 중심 candidate를 준비한다. 실행 모드는 DIRECT, runtime 권한은 read-only, durable handoff는 yes다.

### Explicit Exclusions

- 제품/skill/helper/test 소스 편집, 역할 삭제, 설치 전환, commit/push, 새 서비스 실행.
- 새 정식 Thesis/Scope/PLAN 작성 또는 승인, 독립 Review/Verify/Probe 호출.
- 실제 LLM 탐지력 평가, 원문의 운영 성능 전제 재측정, 새 candidate의 완료 또는 채택 판정.
- 전역 OMP 지침·모델 설정 변경, Observatory의 evidence registry 확장.

## Repository Binding

Relevant Modified Paths:
- None

Relevant Untracked Paths:
- None

Connected Repository Surface:
- iis-workflow, scope-plan, scope-implement, scope-verify, production-heuristic-probing의 호출·결과·완료 기록 계약.
- product-thesis와 scope-shaper의 역할 의존 문장.
- scripts/sync_installed_iis.py와 설치 release/managed links.
- 관련 tests, evaluation/ready-verification의 현행 도구·scenario와 Observatory next_work/독립 sync.
- 이번 artifact는 위 clean 상태 측정 이후 생성했다. 원문 파일은 변경하지 않았다.

## Investigation Frontier

| Area | Question | Disposition | Result |
| --- | --- | --- | --- |
| FLOW_AND_READBACK | 새 흐름으로 대체할 현행 admission/recording 책임은 어디에 있는가? | INVESTIGATE | Plan Review와 Verifier가 현재 필수이며 recording은 scope-verify에 있다. 옮긴 뒤 삭제해야 한다. |
| STATE_AND_AUTHORITY | source/install/loaded identity와 기존 설치 검사는 분리되어 있는가? | INVESTIGATE | check_install 분리는 이미 수정됐다. inspect는 candidate topology를 계속 적용한다. loaded identity는 미관측이다. |
| ALTERNATE_PATHS | standalone, rollback, Thesis refinement가 주 경로 설명을 깨뜨리는가? | INVESTIGATE | standalone dispatch 불일치, 미래 topology inspect 거부, exploration.md 잔존 의존성을 확인했다. |
| EVIDENCE_ALIGNMENT | 테스트·평가·실제 설치가 문서의 주장과 맞는가? | INVESTIGATE | 설치 54개 파일은 소스와 일치한다. phrase tests/label scorer는 행동 증거가 아니며 새 평가 결과는 없다. |

## Current System Model

### Entry And Execution Flow

현재 요청 → bound Thesis/Scope → Plan + independent ADMIT → authorized implementation/self-check → independent whole-Scope VERIFIED → exactly one independent Probe → Main status-only recording. Stage-only와 standalone 경로는 별도 종료 경계를 유지한다. 제안된 새 구조는 아직 candidate 설계이며 현재 호출 권한을 바꾸지 않는다.

### State, Data And Lifecycle Ownership

Thesis는 제품 의미, Scope는 현재 Outcome/Acceptance, Plan은 방법을 소유한다. 현재 Verifier는 의미 판정, Probe는 잔여 반례 탐색, Main은 dispatch/fan-in/currentness와 조건부 상태 기록을 소유한다. 설치 store는 release/manifest/current/installed.json/managed links를 소유하며 제품 효과의 rollback을 수행하지 않는다.

### Configuration And Extension Boundaries

현재 설치 host는 OMP `/home/user01/.omp/agent`다. OpenCode 설정은 조사·변경하지 않았다. 별도 sync_installed_observatory.py는 `/home/user01/.codex/skills/iis-observatory` 두 파일을 복사하는 Codex 경로이며 현재 OMP assurance 전환을 위해 실행할 필요가 없다. Main의 실제 invocation 입력은 skill 절차이며 이 저장소의 범용 dispatch runtime이 아니다.

### Observable Results And Readbacks

설치 inspect의 실제 출력, source→installed rebase 비교, 원본 role 문서와 status-write 계약을 근거로 삼았다. 독립 worker의 실제 준수, 새 topology의 탐지력 또는 설치된 모든 세션의 contract reload는 관측하지 않았다.

## Verified Findings

### Finding 1 — 원문 기준 이후의 변경은 세 파일이다

Classification: FACT
Statement: 61040d24..c856dd72 diff는 scripts/sync_installed_iis.py, tests/test_ready_ticket_boundary_tools.py, observatory/bin/iis-observatory의 세 파일, 10 insertions/2 deletions다. 원문이 설명한 역할 topology는 그대로이며 installer 관련 사실은 일부 갱신해야 한다.
Primary Evidence: A1, A2
Counterevidence Checked: 현재 README와 역할 entry/dispatch를 직접 읽어 assurance 전환이 이미 반영된 상태가 아님을 확인했다.
Boundary / Limitation: 로컬 main과 제공 원문 기준의 비교다. 원격 branch HEAD의 현재성은 주장하지 않는다.
Planning Relevance Candidate: CURRENT_STATE
Freshness Sensitivity: ANCHOR_LOCAL

### Finding 2 — old install 검사 분리는 이미 수정됐다

Classification: FACT
Statement: check_install:201은 check_candidate_release가 아닌 check_release를 호출한다. 기존 release의 자기 manifest·file set·digest·mode 및 managed link 검사를 유지하며 새 REQUIRED를 적용하지 않는다. 관련 회귀는 tests/test_ready_ticket_boundary_tools.py:189-195에 있다.
Primary Evidence: A2, A3, A4
Counterevidence Checked: activate는 신규 candidate를 check_candidate_release로 검사하고 기존 설치를 check_install로 검사한다. old integrity 검사를 제거한 수정이 아니다.
Boundary / Limitation: 신규 topology의 frozen-v4 upgrade→rollback→remove 전체 경로 검증은 아직 별도 필요하다. 이번에 test suite를 실행한 것은 아니다.
Planning Relevance Candidate: PLANNING_CONSTRAINT_CANDIDATE
Freshness Sensitivity: ANCHOR_LOCAL

### Finding 3 — inspect에는 미래 topology 혼용 경로가 남는다

Classification: FACT
Statement: inspect는 bundle 인자가 없으면 check_install을 거친 뒤에도 check_candidate_release를 호출한다. 메모리에서 REQUIRED에 iis-workflow/tools/assurance.py를 추가했을 때 현재 설치의 check_install은 성공했지만 inspect는 ValueError: incomplete skills candidate manifest로 끝났다.
Primary Evidence: A3, A5
Counterevidence Checked: REQUIRED를 변경하지 않은 현재 실제 CLI inspect는 성공했다. 현 설치 손상으로 오인하지 않았다. 실험 종료 시 메모리 상수를 복원했고 운영 파일/링크는 바꾸지 않았다.
Boundary / Limitation: 이는 새 도구가 구 설치본을 inspect할 때의 조건부 경로다. 실제 신규 bundle activation/rollback을 수행한 결과는 아니다.
Planning Relevance Candidate: PLANNING_CONSTRAINT_CANDIDATE
Freshness Sensitivity: ANCHOR_LOCAL

### Finding 4 — 설치 바이트와 소스는 일치하지만 세션 reload 증거는 아니다

Classification: FACT
Statement: 실제 inspect는 bundle f41ceb4e8fcb8e29234fdb9c354ce20c03219a667009527c8c63a83e90bc1770, protocol 4, files 54, loaded_identity NOT_CHECKED를 반환했다. 현재 source payload 54개를 installer rebase 후 설치 바이트와 비교한 불일치는 0개다.
Primary Evidence: A3, A5
Counterevidence Checked: bundle.json의 source 문자열은 /home/user01/tmp/iis-activate-c856dd7이므로 이름만으로 일치를 판단하지 않고 실제 rebase 결과를 비교했다.
Boundary / Limitation: 다른 세션의 loaded contract, 행동 준수, 성능은 이 검사로 증명되지 않는다.
Planning Relevance Candidate: CURRENT_STATE
Freshness Sensitivity: RUNTIME_STATE

### Finding 5 — 삭제 전에 옮겨야 할 recording과 권한 경계가 존재한다

Classification: FACT
Statement: 현행 scope-verify/SKILL.md:73-85에 fan-in, Probe, effects settlement, 원본 currentness, ready→done status-only write/readback과 전체 요청 구분이 묶여 있다. Planner/Implementer에는 conditional first work, actor continuity, material-method re-entry 및 correction reproducer 규칙이 존재한다.
Primary Evidence: A6, A15
Counterevidence Checked: standalone verification은 Plan 없이 진입할 수 있다. 이 예외를 정상 implementation admission의 자동 면제로 확장할 수 없다.
Boundary / Limitation: 절차 계약이지 host-enforced semantic engine 또는 locking/authentication 보장이 아니다.
Planning Relevance Candidate: PLANNING_CONSTRAINT_CANDIDATE
Freshness Sensitivity: ANCHOR_LOCAL

### Finding 6 — T10 파일 목록에 load-bearing reference가 빠졌다

Classification: FACT
Statement: 원문 T10은 product-thesis/SKILL.md를 적었지만 product-thesis/references/exploration.md는 적지 않았다. 그 reference:83은 independent method review를, :97은 scope-verify의 fresh whole-Scope verdict 및 observation retention을 계속 요구한다.
Primary Evidence: A1, A7
Counterevidence Checked: product-thesis SKILL의 bounded exploration이 해당 reference를 사용하는 구조와, 원문 T10이 제품 의미 변경이 아니라 topology 연결 수정을 의도한다는 점을 확인했다.
Boundary / Limitation: 이 참조 두 곳을 수정 대상에 추가하는 것으로 충분하다. Thesis의 semantic frontier나 source revision 모델 전체를 재설계할 이유는 발견하지 못했다.
Planning Relevance Candidate: PLANNING_CONSTRAINT_CANDIDATE
Freshness Sensitivity: ANCHOR_LOCAL

### Finding 7 — dispatch는 문서 계약이며 standalone 모순이 실제로 있다

Classification: FACT
Statement: Probe SKILL:46 및 result:102는 standalone investigation을 허용하지만 dispatch/probe.md:10-11은 completed verifier result를 Required로 요구한다. 공통 dispatch는 owning contract 우선을 명시한다. test_delegated_role_dispatch는 문구와 payload 포함을 검사하며 실제 agent 호출 payload 생성·초기 접근 순서를 실행하지 않는다.
Primary Evidence: A8, A9, A14
Counterevidence Checked: 공통 projection 목록의 purpose-first-review/dispatch/reviewer.md는 Plan Reviewer와 다른 일반 검토 역할이다. reviewer라는 이름만으로 전부 삭제하면 안 된다.
Boundary / Limitation: 조사한 workflow/dispatch/tests 및 README가 명시한 host-independent 표면에 관한 사실이다. 외부 OMP 내부 구현의 모든 기능 부재를 주장하지 않는다.
Planning Relevance Candidate: PLANNING_CONSTRAINT_CANDIDATE
Freshness Sensitivity: ANCHOR_LOCAL

### Finding 8 — 기존 scorer는 label-only이며 causal fixture는 별개다

Classification: FACT
Statement: score_result.score는 parsed_verdict, missing/duplicate cases, repetitions와 보고된 target_mutated를 집계한다. run_id는 있는 경우에만 검사하고 target_mutated 누락은 truthy로 세지 않는다. 실제 raw causal evidence는 분석하지 않는다. fixture boundary tests에는 실제 CLI entry와 defective/normal parser twin을 구분하는 assertion이 있다.
Primary Evidence: A10, A11, A12
Counterevidence Checked: README가 scorer를 label-only로 정확히 한정한다. 이를 기존 구현 결함이나 현재 LLM 탐지 성능 측정으로 재분류하지 않았다.
Boundary / Limitation: fixture 코드를 읽었으며 이번 조사에서 해당 테스트나 LLM cohort를 실행하지 않았다.
Planning Relevance Candidate: PRODUCT_DEPENDENCY_CANDIDATE
Freshness Sensitivity: ANCHOR_LOCAL

### Finding 9 — 원문 closure 식의 직접 관측 성공 조건이 불명확하다

Classification: FACT
Statement: 원문:487-502의 식은 required 직접 관측의 존재와 gate의 완료·성공을 적지만 직접 관측 판별 조건의 충족을 별도 conjunct로 적지 않는다. 원문:240-242는 직접 제품 결과 관측을 요구하고 :262는 기존 요구에 따른 판별 조건을 요구하므로 전체 의도는 단순 파일 존재보다 강하다.
Primary Evidence: A1
Counterevidence Checked: 원문:498-499의 unresolved counterexample/gap 차단도 확인했다. 그러나 observation 자체의 실패를 Prober finding으로 변환해야만 차단되는 구현을 요구한 것으로 읽으면 안 된다.
Boundary / Limitation: 미구현 설계의 serialization 명확화 문제다. 현재 helper에서 false completion이 재현됐다는 주장이 아니다.
Planning Relevance Candidate: PLANNING_CONSTRAINT_CANDIDATE
Freshness Sensitivity: EXTERNAL_VERSION

### Finding 10 — Observatory 확장 없이 기존 read model을 유지할 수 있다

Classification: FACT
Statement: next_work.py:57-89는 ready만으로 다음 delivery 단계를 정하지 않고 done도 해당 Scope 기록으로 한정한다. 별도 sync_installed_observatory.py는 Codex 설치의 SKILL.md와 agents/openai.yaml만 복사한다.
Primary Evidence: A13
Counterevidence Checked: done에서 remaining_required_outcomes가 있으면 재조정을 안내하며 다음 Scope를 실행하지 않는다. Stage.COMPLETE 필드만 보고 전체 요청 성공으로 해석하지 않았다.
Boundary / Limitation: scanner 전체와 모든 Observatory 테스트를 실행하지 않았다. 상태 의미와 sync 경계를 결정하는 직접 경로를 확인했다.
Planning Relevance Candidate: CURRENT_STATE
Freshness Sensitivity: ANCHOR_LOCAL

## Inferences

### Inference 1 — 직접 관측 실패를 closure의 독립 차단 입력으로 만들어야 한다

Statement: observation 존재 여부만 구현하면 명시적 요구를 위반한 readback이 있어도 gates 성공/no-finding과 함께 closure될 수 있다. 각 observation이 기존 판별 조건을 충족했는지, 위반했는지, 관측 불가능했는지를 보존하고 충족만 완료 입력으로 삼는 것이 원문 목적에 맞는다. 새로운 전체 semantic judge는 필요하지 않다.
Supported By: Finding 9
Plausible Alternative: observation 실패가 반드시 unresolved material counterexample으로 변환되는 단일 기존 producer 계약을 정의할 수도 있다. 어느 방식을 쓰든 실패·unknown이 존재만으로 수용되는 경로가 없어야 한다.
Evidence Needed To Disprove: T01 데이터 계약과 T02 실행 결과에서 실패/unknown 직접 readback이 다른 no-finding과 무관하게 BLOCKED되고 정상 twin은 닫히는 구체적 discriminator.
Planning Relevance Candidate: PLANNING_CONSTRAINT_CANDIDATE

### Inference 2 — dispatch 검증을 위해 범용 runtime을 추가할 이유는 없다

Statement: T07/T12/13.1의 실제 payload·공개 순서 검사는 실제 host invocation 기록을 대상으로 해야 한다. skill 문자열이나 새 helper에 넣은 인공 payload의 통과만으로 Main이 올바른 입력을 보냈다고 주장할 수 없다. production helper는 schema/currentness/closure에 한정하고 행동 평가에서 실제 dispatch를 보존하는 편이 목적과 유지 비용에 맞는다.
Supported By: Finding 7, Finding 8
Plausible Alternative: 나중에 host 전용 dispatch builder가 명시적으로 승인되면 그 생성 payload를 executable tests로 검사할 수 있다. 현재 문서가 배제한 scheduler/범용 harness를 이번에 암묵 도입하지 않는다.
Evidence Needed To Disprove: 현재 실제 dispatch 생성·호출 경로가 candidate 소스에 존재하고 일반 호출자가 이를 반드시 사용하는 직접 실행 증거.
Planning Relevance Candidate: PLANNING_CONSTRAINT_CANDIDATE

## Alternate, Legacy And Bypass Paths

- 기존 설치 check_install과 candidate 검사 분리: 이미 수정된 정상 upgrade admission을 확인했다.
- future REQUIRED를 적용한 inspect: 기존 설치 자체는 유효하지만 candidate 기준 재검사에서 거부되는 조건부 경로를 실행했다.
- standalone Probe: SKILL 허용과 dispatch Required의 모순을 확인했다.
- Thesis refinement: exploration.md의 현행 Review/Verifier 의존성이 T10의 명시 파일 목록을 벗어남을 확인했다.
- 일반 purpose-first-review projection은 삭제 대상 Plan Reviewer와 구분했다.
- Observatory done/ready와 독립 Codex sync를 OMP completion authority로 오인하지 않았다.

## Documentation, Tests And Runtime Alignment

- 원문 조사 이후 installer 수정이 들어왔다. T11의 원인 설명 일부는 역사적 사실로 유지하되 실제 작업은 현재 코드에 맞춰 좁혀야 한다.
- source와 installed bundle은 경로 rebase 후 일치한다. 실행 중 세션 reload와 agent compliance는 별개다.
- 현행 contract tests의 다수는 문구를 고정한다. 재설계 시 해당 문구를 새 문구로 재고정하는 방식은 피하고, payload/path 구조 검사와 실제 helper 동작·fixture 구분 검사를 분리한다.
- delegated/corrective scenarios는 NOT_RUN이다. 기존 역사적 평가 결과를 새 topology의 측정치로 옮겨 쓰지 않는다.

## Absence Claims

### Absence Claim 1

Claim: 조사한 current workflow source에서 신규 assurance reference/helper와 실제 agent dispatch runtime을 찾지 못했다.
Search Universe: iis-workflow/**, common/role dispatch 문서, tests/test_delegated_role_dispatch.py, README, evaluation/ready-verification/README.md의 current sections.
Method: 디렉터리 glob, assurance/role 관련 bounded grep, 파일 본문 및 현행 도구 목록 확인.
Result: iis-workflow에는 SKILL.md, references/delegated-role-dispatch.md, templates/BASELINE-NNN.template.md가 있으며 tools/assurance.py 및 references/assurance.md는 없다. dispatch 테스트는 문구·packaging 검사다.
Boundary: 저장소 외부 host 기능 전체에 대한 부재 주장이 아니다.

## Contradictions And Surprises

- 원문:155-161의 check_install→check_candidate_release 주장은 조사 당시에는 맞았지만 현재 c856dd7에는 맞지 않는다.
- 그 수정과 별개로 inspect는 여전히 candidate 적합성을 적용한다. 완료된 수정과 남은 변경을 분리해야 한다.
- T10의 entry-only 변경으로는 exploration.md를 통한 old Review/Verifier 책임 재유입을 막지 못한다.
- 원문은 host 강제를 과장하지 않지만 일부 test 계획은 실제 generator/access-log가 이미 있는 듯 읽힐 수 있다. 실제 실행 증거와 정적 계약 검사를 분리해야 한다.

## Review Verdict And Preparation Candidates

### 판단

REQUIRED CHANGE — 전체 설계 폐기가 아니라 현재 기준 작업계획의 보정이 필요하다. Reviewer/Verifier 제거를 다른 LLM Aggregator로 대체하지 않는 방향, 실제 관측과 반례 탐색 분리, effect settlement 유지, Scope/Thesis/Transition authority 보존, Observatory 비확장은 이번 검토 범위에서 별도 결함을 확인하지 못했다. 탐지력 향상은 아직 가설이다.

### 최소 보정

1. T11의 이미 완료된 check_install 수정을 제거하고, installed inspection과 candidate admission 구분 및 frozen old-v4 전환 회귀에 집중한다. inspect의 bundle 명시 모드가 candidate 적합성을 검사하는 기존 의미는 유지할 수 있다. bundle 없는 installed inspection에는 자기 manifest 기준을 적용하는 것이 좁은 수정 후보다.
2. T10에 product-thesis/references/exploration.md:83,97을 추가한다. 일반 purpose-first-review는 유지한다.
3. T01/T02에서 required observation의 만족/위반/관측 불가와 required gate 성공을 각각 정의한다. native raw output/identity와 연결하며 단순 LLM boolean을 증거로 삼지 않는다. 사람 판단이 필요한 결과는 기존 계약이 요구하는 확인을 확보하고 없으면 BLOCKED로 남긴다.
4. T07/T12의 정적 검사, helper contract 검사, 실제 agent dispatch/embargo 행동 검사를 분리한다. 검사 편의를 위해 범용 runtime·scheduler·DB를 새로 만들지 않는다.

### 정책 선택과 구현 세부사항의 구분

- 원문:325의 clean committed target 기본안은 단순하지만 commit 권한 없는 정상 작업은 TARGET_NOT_SEALED로 멈춘다. 이는 숨길 비용이 아니라 사용자에게 드러낼 제품 정책 선택이다. 이번 준비에서는 자동 commit 권한이나 uncommitted manifest 지원을 채택하지 않았다. 원문 기본안을 쓰려면 정식 제품 경계에 명시하고, commit 없이 completion이 필수라면 T01/T02의 target 형식을 구현 전에 바꿔야 한다.
- stable product target과 evidence output/status-only recording을 구분한다. evidence 파일 생성 때문에 target 자체가 달라졌다고 처리하거나, 그 반대로 executable/config 변경을 effect 예외로 숨겨서는 안 된다.
- Baseline은 별도 skill/승인 artifact가 아니라 Plan의 machine-readable block 또는 assurance-only invocation 입력이다. 정확한 block bytes를 외부 binding에서 hash하고 자기참조 digest를 넣지 않는다.
- required denominator는 실행 전 고정한다. optional lane도 실제 finding/effect를 만들었다면 closure에서 사라지지 않는다. correction 뒤 새 target에는 completion용 결과를 다시 획득하는 원문 초기안을 유지한다.
- 모델/effort, lane 수·예산, cohort 반복·수치 채택 기준은 이번 요청에서 선택되지 않았다. 검토·준비에 필요한 누락 정보는 아니며, 향후 해당 delegated 실행/평가를 시작하기 전에 현재 사용자 선택을 확보할 항목이다.

### T01–T17 착수 매핑

다음은 구현 완료 표가 아니라 다음 실행을 위한 후보 작업표다. 원문 작업 ID를 모두 보존했다.

| ID | 현재 기준 실제 대상·결정 | 선행·통합 경계 | 관측할 완료 조건 |
| --- | --- | --- | --- |
| T01 | 신규 iis-workflow/references/assurance.md. 원문 최소 데이터와 직접 observation outcome, Baseline block bytes, source/execution/effect 경계를 명시한다. | 다른 worker보다 먼저 공통 계약을 고정하되 별도 승인 역할은 추가하지 않는다. | 동일 input에서 missing/violation/unknown과 정상 control의 closure 결과를 명확히 예상할 수 있다. |
| T02 | 신규 iis-workflow/tools/assurance.py. validator/binding/native evidence 연결/closure만 소유한다. | T01 소비. 제품 harness·agent scheduler·status writer는 제외한다. | helper 실제 호출에서 누락·stale·wrong target·실패 관측·unknown settlement를 거부하고 정상 사례는 수용한다. |
| T03 | scope-plan/SKILL.md와 references/plan.md를 method+Baseline 작성으로 정리한다. | T01. Code Grounding/conditional first work는 보존한다. | Plan 결과가 독립 ADMIT 없이 반환되고 unresolved effect 전제가 dependent mutation을 허용하지 않는다. |
| T04 | scope-plan/references/review.md와 dispatch/reviewer.md 삭제; planner projection·agents/openai.yaml 수정. | T03 및 T11 최종 payload와 결합한다. | Plan Reviewer/v2 artifact lifecycle 의존성이 active 경로에서 사라진다. purpose-first-review는 남는다. |
| T05 | scope-implement entry/reference/dispatch/agent의 admission과 correction을 current Plan/Baseline으로 전환한다. | T01–T04. actor continuity와 handoff를 보존한다. | self-check가 final completion을 만들지 않고 material method change는 Planner로, 구현 correction은 기존 actor로 돌아간다. |
| T06 | Probe entry/dispatch/agent를 targeted/scout 가능한 lane으로 변경한다. | T01. standalone과 completion lane의 입력을 분리한다. | post-VERIFIED/exactly-one 강제가 없어지고 실제 attack·raw evidence·outcome와 COMPLETE/PARTIAL/BLOCKED가 분리된다. |
| T07 | common delegated-role-dispatch.md에 lane roster·초기 projection·후기 narrative 공개를 연결한다. | T04/T06. source/safety는 처음부터 제공한다. | 실제 invocation 기록으로 hypothesis 전 peer/구현 narrative 누출 여부를 판단한다. 단순 문구 test로 증명하지 않는다. |
| T08 | iis-workflow/SKILL.md의 flow/re-entry/completion을 전환하고 scope-verify의 recording을 이전한다. | T02–T07. Main integration owner가 공유 경계를 통합한다. | EVIDENCE_COMPLETE만으로 무단 status write가 생기지 않으며 stage-only/whole-request 경계와 status-only readback이 유지된다. |
| T09 | scope-verify active directory/payload 삭제. | evidence/recording 이전 후 T11과 일관된 candidate로 통합한다. | retired verifier 경로를 active caller/agent/payload가 요구하지 않는다. historical evidence는 보존한다. |
| T10 | scope-shaper/SKILL.md, product-thesis/SKILL.md에 더해 product-thesis/references/exploration.md:83,97 수정. validator/Transition template는 유지한다. | T08. 제품 의미나 Scope schema는 바꾸지 않는다. | refinement 경로에서도 old reviewer/verifier가 재유입되지 않고 Thesis authority와 역사 보존은 그대로다. |
| T11 | sync_installed_iis.py의 payload/required/retired 목록과 installed inspect 경계를 수정한다. check_install 수정은 재작업하지 않는다. | T02–T10의 실제 최종 payload. frozen v4 fixture는 baseline 상수와 독립시킨다. | disposable roots에서 old-v4→new→inspect→rollback→installed inspect→remove, 중간 실패 복구, user entry 보존을 관측한다. |
| T12 | 네 contract test 파일의 phrase-pinning을 제거/축소하고 실제 helper·payload/path 경계로 교체한다. causal fixtures는 유지한다. | T02–T10. 공유 tests는 한 통합 owner가 편집한다. | 실패 관측, wrong binding, missing lane, material finding, unknown effect가 false green을 만들지 않고 정상 twin도 통과한다. |
| T13 | 기존 failure_space_fixtures.py, fixture_catalog.py, fixture-boundary tests를 재사용하고 assurance cases/scenarios를 추가한다. | T01/T06/T12. toy verdict와 실제 causal readback은 구분한다. | wrong-entry, weak oracle, omitted obligation, framing, state/identity, effect contamination을 정상 twin과 구별한다. |
| T14 | 신규 score_assurance.py와 필요한 evaluation tests. score_result.py는 역사적 label scorer로 유지한다. | T13. fixture causal oracle 및 offline evidence disposition의 입력을 명확히 한다. | raw locator 존재만이 아니라 case별 discriminator 근거, 전 run denominator와 비용을 집계한다. production done credential을 만들지 않는다. |
| T15 | delegated/corrective scenarios와 evaluation README current 부분을 갱신한다. | T06–T09/T13/T14. historical sections는 편집하지 않는다. | removed review/whole-Scope expectations만 교체하고 originals/accessibility/reproducer 목적은 유지한다. 미실행은 NOT_RUN이다. |
| T16 | README, PRODUCT-COMPLETION-BRIEF, dependency-map, runtime verification, host integration current 설명을 맞춘다. | T08–T15 통합 후 current references를 확인한다. | caller가 다시 old Review/Verifier를 요구하지 않고 installed bytes/loaded contract/behavior의 구분을 유지한다. |
| T17 | Observatory next_work/scanner/skill 및 독립 sync의 기존 의미를 유지한다. | T16 용어 일관성 확인만으로 시작한다. | ready만으로 자동 next step을 만들지 않고 done을 whole-request success로 확장하지 않는다. OMP 전환을 위해 Codex sync를 실행하지 않는다. |

### 실행 순서 후보와 공유 소유권

1. T01의 data/closure 계약 및 target 정책을 먼저 명확히 한다. 새 closure가 자기 설계를 스스로 승인하는 형태로 현재 실행 계약을 우회하지 않는다.
2. 실제 구현을 승인받으면 T02와 역할 변경을 진행한다. 이후 분업 시 core/helper(T01–T02), method/implementer(T03–T05), Probe(T06), integration/workflow/authority(T07–T10), packaging(T11)의 소유권을 나누되 공통 data schema 변경은 Main 통합 결정으로 취급한다. 같은 파일을 공유하는 T12–T16은 integration 시점에 조율한다.
3. T11–T17까지 하나의 coherent candidate를 만든다. 부분 운영 설치는 하지 않는다. 소스 검사와 disposable packaging 검증은 통합 뒤 한 번 실행한다.
4. 실제 행동 평가를 실시한다. 실험 A는 동일 구현의 assurance 비교, 실험 B는 Reviewer 유무가 구현 자체에 미치는 end-to-end 비교다. 서로 합쳐 해석하지 않는다.
5. 원문의 ablation 일곱 축을 보존한다: machine only/one Probe, one/targeted+scout, full narrative/embargo, full Baseline/lane projection, same/different models, equal total/equal per-lane budget, correction 재획득 비용. 비용이 이유라면 범위 축소를 사용자 결정으로 남기고 조용히 생략하지 않는다.
6. 모델·effort·예산·반복·수치 채택 기준을 결과 전에 고정한다. 정상 control의 false block, 미실행/timeout/cancelled와 재실행 비용을 denominator에서 빼지 않는다. 채택과 운영 activation은 별도 현재 권한 아래 수행한다.

### 첫 구현에서 필요한 판별 사례

- required observation raw result는 있으나 promised readback이 위반됨 → BLOCKED; 동일 경로 정상 readback → EVIDENCE_COMPLETE 후보.
- gate command는 0 종료이나 required job은 skipped/미실행 → BLOCKED.
- 다른 source/artifact/runtime/mechanism 결과, 누락 lane, duplicate invocation → BLOCKED.
- 여러 no-finding + 한 material finding 또는 unresolved candidate → BLOCKED.
- optional lane이라도 effect가 시작됐고 정산 불명 → BLOCKED.
- 정상 artifact-only/standalone assurance에 가짜 production runtime/fresh implementation Plan을 요구하지 않는다.
- status-only ready→done은 preimage와 허용 delta/readback으로 연결하며 제품/Acceptance 변경은 별도로 거부한다.
- frozen old-v4 설치를 신규 topology 도구로 관측하고 rollback 후에도 current installation integrity를 읽을 수 있다.
- 실제 hypothesis/dispatch evidence에서 초기 구현 narrative/peer result 누출을 구별한다. 접근 차단 없는 host는 절차적 embargo라고 보고한다.

### 예정 검증 명령 — 이번에는 실행하지 않음

```text
python3 -B -m unittest discover -s tests
PYTHONPATH=observatory/src python3 -B -m unittest discover -s observatory/tests
```

packaging prepare/inspect/activate/rollback/remove는 승인된 disposable store/client roots에서 먼저 수행한다. 운영 store는 이번에 inspect만 했다. 새 candidate의 helper/CLI 명령 형태는 T01/T02에서 정해야 하므로 미구현 명령을 실행 가능한 것처럼 제시하지 않는다.

## Reproducible Read-Only Evidence

### 실제 설치 검사

실행한 명령:

```text
python3 -B scripts/sync_installed_iis.py inspect --store /home/user01/.local/share/iis
```

관측 결과:

```json
{"bundle_id":"f41ceb4e8fcb8e29234fdb9c354ce20c03219a667009527c8c63a83e90bc1770","family":"iis-skills","protocol":4,"files":54,"loaded_identity":"NOT_CHECKED"}
```

### 미래 topology 관측 경계 실험

Python eval에서 sys.dont_write_bytecode=True로 현재 installer를 importlib로 로드했다. 파일/링크를 변경하지 않고 module.REQUIRED만 잠시 확장한 뒤 finally에서 복원했다.

```python
old_required = installer.REQUIRED
try:
    installer.REQUIRED = old_required + ('iis-workflow/tools/assurance.py',)
    print(installer.check_install(store)['bundle_id'])
    try:
        installer.inspect(store)
    except ValueError as exc:
        print(type(exc).__name__, str(exc))
finally:
    installer.REQUIRED = old_required
```

출력은 check_install의 현재 bundle ID와 `ValueError incomplete skills candidate manifest`였다. 미래 candidate를 설치한 것이 아니라, 현 코드에서 검사 경계가 다름을 직접 구분한 실험이다.

### source/install 바이트 비교

installer.payload_files(root) 54개에 대해 installer.rebase(file bytes, installer.source_origins(root), exact release path)를 계산하고 대응 installed file bytes와 비교했다. mismatches는 `[]`였다. manifest와 mode/current/links 검사는 실제 CLI inspect가 담당했다. 이 비교는 source package identity 증거이지 invocation-loaded identity 증거가 아니다.

## Coverage Boundary

Inspected:
- 제공 원문 전체, exact baseline→current Git delta, 현행 역할 entry/핵심 reference/dispatch와 직접 연결된 authority reference.
- installer 실제 validation/activation/rollback 경로, 설치 manifest/links, live inspect 및 메모리-only 미래 REQUIRED 반례.
- 관련 test의 실제 assertion, evaluator score 함수와 current scenario 상태, Observatory deciding status path 및 독립 sync.

Not Inspected:
- 새 candidate 구현/실행, 전체 test suite, LLM behavioral cohort, 타 세션의 contract reload.
- 무관한 historical evaluation raw data, 원격 branch 현재성, 외부 제품/production 시스템.

Why Current Coverage Is Sufficient Or Insufficient:
현재 문서 기반 착수 준비를 바꾸는 근거인 baseline drift, 실제 설치 identity, 삭제 역할의 caller/recording/refinement 의존성, executable-vs-procedural 검사 경계를 직접 확인했고 standalone/inspect/refinement counterpath로 최초 모델을 도전했다. 새 구조의 품질 채택에는 아직 구현과 실제 행동 평가가 필요하지만 그것은 이번 현재-state 조사 범위의 미확인 사실을 완료로 꾸민 것이 아니라 명시적으로 수행하지 않은 후속 단계다. 모델·예산·commit 정책 역시 준비 완료를 막는 repository unknown이 아니라 향후 권한·제품 선택이다.

## Completion

Investigation Completion: COMPLETE
Decision-Critical Unknowns: None
