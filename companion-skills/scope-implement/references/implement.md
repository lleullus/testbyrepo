# Scope Implementer: implementation core

## 1. Input fixity and product preflight

Before mutation, read every bound Thesis, exact ready Scope, applicable Transition and current Plan/Assurance Baseline. Validate source/structure through the supplied same-snapshot tools and executor-owned fixed refs; do not create a checksum layer. Preserve current user stage/effect/stop authority and selected actor/model/effort. Recheck before returning COMPLETE.

Trace the real entry to state/effect owner and authoritative result. Separate existing behavior and pre-existing worktree changes from this delta. Read frontier and predecessor evidence as navigation, not new requirements. A missing/contradictory original returns to its owner; do not repair product meaning in implementation.

## 2. Native execution and conditional first work

Use ordinary read/edit/build/test/CLI and native service handles. No lease, queue, scheduler or new approval stage. Services require actual readiness and final settlement; never adopt/stop shared services without authority. Settled command failure is an ordinary result, not a generic workflow lock.

For each conditional method bundle execute only permitted_initial_work, observe the declared discriminator and stop dependent work if refuted/unknown. Safe unrelated work requires independent ownership/state/effects. Destructive/irreversible/non-idempotent effects require the exact current permission and preconditions before mutation. Lost response requires applied/not-applied authoritative readback, not blind retry.

## 3. Method boundary

Implement the smallest coherent change satisfying Outcome, applicable Acceptance, failure/recovery, preservation and bound Thesis construction decisions. Equivalent naming/private helpers and equivalent local structure are local discretion only when they preserve those decisions and the Plan method. Discovery of a fact, one regression or small diff is not alone material revision; conversely, small/private/technical does not make a decision implementation-local.

If an admission-controlling premise changes or cannot be established, stop dependent work and record the exact bound source/Plan anchor, prior premise, new observation and affected owner/interface/effect/readback. Return to Product-Thesis when the product success/preservation/identity decision or a Thesis-bound construction decision/premise must change; to Transition when intermediate order/authority/invariant must change; to Planner when only its method/oracle/safety premise changes. Do not edit Baseline yourself. Fallback/retry/cleanup/recovery must not decide a different user result. Revision never silently changes implementing actor/model/effort or expands current authority.

## 4. Implementation loop and self-check

Select the cheapest actual discriminator from the existing project path. A transition trace must reach the contract-allowed recovery/terminal state and minimum subsequent operation or authoritative readback. Preserve the same relevant identity; never reset away residue unless reset is the promised recovery. Helpers, unit tests and source inspection are supporting evidence when the actual result boundary differs.

For a material predecessor finding, preserve runnable content or a durable executor-owned fixed locator, initial state, mechanism identity, failed readback, observation window and cleanup. Prefer promoting the same discriminator to an existing project regression when it protects against a plausible recurrence; confirm defective/corrected behavior, not merely a green test. If dangerous production replay is required, use original evidence/current authoritative state and permitted disposable reproduction instead. Preserve the exact evidence limit.

Investigate only directly evidenced same-assumption siblings sharing parser/owner/state/token/helper. Do not sweep unrelated similarly named paths or create a failure registry. Every supplied/self-discovered in-scope finding is directly fixed, fixed by the same evidenced cause, separately fixed, or returned with exact owner/constraint. A named finding is not a closed finding.

### Execution handoff

Preserve actual build/launcher/command/config/fixture/readback/reset/cleanup recipes and artifact/runtime/mechanism identities when non-obvious. Reusing setup/build does not preserve scenario state or prior success. Keep executable reproduction accessible after invocation ends, not only in expiring /tmp or scrollback. Do not copy credentials/protected data into evidence.

Prefer reuse, deletion and consolidation before layers. Do not add fallback, persistence, telemetry, recovery guarantees or unrelated cleanup without a contract anchor/necessary realistic failure path.

## 5. Binding and completion

Under `../../../iis-workflow/references/assurance.md`, native self-check gate evidence is reusable only for the same final sealed target/binding. Mid-edit tests remain self-check navigation. No automatic commit occurs; unsealed working trees are reported accurately. Separate source target from build/runtime/mechanism identity and declare all mutable effects.

Before COMPLETE confirm exact originals/Plan/Baseline/current authority; implemented behavior and known finding dispositions; real changed-path observations and required follow-up state; usable correction/regression/sibling evidence; accessible execution handoff; settled effects and pre-existing mutation attribution. Preserve remaining material limits instead of claiming success.

Return the entry contract's concise SCOPE IMPLEMENT RESULT. Never change Scope Status, run independent attack lanes as your own assurance, carry forward old completion labels or claim a whole-Scope semantic verdict. New target completion evidence is reacquired; prior traces remain explanation and reproduction only.
