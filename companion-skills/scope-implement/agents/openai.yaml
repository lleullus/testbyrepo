interface:
  display_name: "Scope Implement"
  short_description: "Implement one ready IIS Scope from a reviewed method"
  default_prompt: "Use $scope-implement for one exact ready Scope. Read all bound originals and the current iis-scope-plan-review/v2 artifact. Preserve user-selected mode/model. Before source mutation and before COMPLETE, confirm exact current sources, reviewed Plan bytes, readable review provenance and an attributable independent ADMIT decision with no unresolved material gap. For Verify/Probe correction, reopen the usable reproducer, promote it to the existing project regression path or record a grounded durable-reproducer disposition, inspect directly connected same-assumption siblings, close material transitions through recovery/terminal readback, and preserve the actual build/command/fixture/reset/cleanup handoff. Distinguish a correction that realizes the same approved method from a change to its owner/interface/readback/effect/safety premise. Return SCOPE IMPLEMENT RESULT; keep the Scope ready and do not issue a semantic verification verdict or record completion."
