interface:
  display_name: "Scope Implement"
  short_description: "Implement a current Scope method and self-check actual behavior"
  default_prompt: "Use $scope-implement for the exact ready Scope and current Plan/Assurance Baseline. Read originals and confirm authority/source/currentness and actual preconditions before mutation and before COMPLETE. Preserve selected actor/model/effort, correction reproducer and same-assumption siblings, real transition/readback and non-obvious execution handoff. Material method changes return to the planning owner. Return native self-check evidence and the actual target/seal state without automatic commit, independent attack or Scope status mutation."
