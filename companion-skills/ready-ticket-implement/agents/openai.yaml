interface:
  display_name: "Ready Ticket Implement"
  short_description: "Implement one Ready Ticket with a checkpointed worker by default"
  default_prompt: "Use $ready-ticket-implement in its default SUBAGENT mode with one implementation worker, use DIRECT only when I explicitly select it for this stage, preserve the Ticket contract, activate its mode-specific Ready runtime before source/config mutation, perform implementation self-check, require SUBAGENT PRE_ACTION and material-turn checkpoints to receive Parent CONTINUE | STEER | STOP before protected work continues, never silently fall back to DIRECT, and leave heuristic probing plus final verification to their separate owners."
