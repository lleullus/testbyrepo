interface:
  display_name: "Ready Ticket Implement"
  short_description: "Implement one Ready Ticket with direct-first checkpointed delegation"
  default_prompt: "Use $ready-ticket-implement to directly implement the exact Ready Ticket by default, preserve the Ticket contract, perform implementation self-check, use SUBAGENT only when I explicitly select it for this stage, require its PRE_ACTION and material-turn checkpoints to receive Parent CONTINUE | STEER | STOP before protected work continues, and leave heuristic probing plus final verification to their separate owners."
