interface:
  display_name: "Ready Ticket Implement"
  short_description: "Implement one Ready Ticket through one worker"
  default_prompt: "Use $ready-ticket-implement to assign the exact Ready Ticket to one implementation worker by default, preserve the Ticket contract, perform implementation self-check, and leave final verification to the separate verifier."
