interface:
  display_name: "Ready Ticket Implement"
  short_description: "Implement one Ready Ticket from current independent method review"
  default_prompt: "Use $ready-ticket-implement with one SUBAGENT worker by default, or current Main only when I explicitly select DIRECT for this stage. Preserve my current model choice and exact Ticket authority. Obtain actual current independent ADMIT from ready-ticket-plan before begin/assignment, pass plan_review_path, confirm load-bearing premises, and start reviewed work without a normal first-change checkpoint. Follow conditional initial scope and material-turn/current-review boundaries, perform actual self-check, settle owned services/effects, leave Ticket ready, and return IMPLEMENT RESULT without assuming final verification authority or silently falling back between modes."
