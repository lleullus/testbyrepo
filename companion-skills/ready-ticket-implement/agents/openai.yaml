interface:
  display_name: "Ready Ticket Implement"
  short_description: "Implement one Ready Ticket with optional concurrent auditors"
  default_prompt: "Use $ready-ticket-implement to implement the specified Ready Ticket with the exact optional Auditor Count and bindings; perform implementation self-check only and do not adjudicate the separate verification authority."
