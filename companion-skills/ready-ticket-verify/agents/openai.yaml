interface:
  display_name: "Ready Ticket Verify"
  short_description: "Verify every Ready Ticket obligation with integrated discovery"
  default_prompt: "Use $ready-ticket-verify with exactly one SUBAGENT verifier by default, or current Main only when I explicitly select DIRECT for this stage. Preserve current selected models and never silently switch modes. Bind actual current authority/target directly, with any execution plan/review as optional navigation. Semantically check every authored flow/AC, derive a bounded internal false-completion frontier, obtain fresh discriminating verifier-owned evidence and preserve the complete authored denominator even when no extra lane exists. Use delegated PRE_RUNTIME, material-turn and VERIFIED-only PRE_PROGRESSION boundaries, settle cleanup/effects, and call ready_guard finalize_verification. Keep evidence verdict, progression result and actual status distinct; do not remediate source or manufacture PASS from prior self-check."
