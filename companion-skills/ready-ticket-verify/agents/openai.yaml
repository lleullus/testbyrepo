interface:
  display_name: "Ready Ticket Verify"
  short_description: "Verify one Ready Ticket with a checkpointed verifier by default"
  default_prompt: "Use $ready-ticket-verify in its default SUBAGENT mode with exactly one verifier for the exact Ready Ticket, use DIRECT only when I explicitly select it for this stage, require a current COMPLETE ready-ticket heuristic-probe handoff plus its exact Probe Machine Binding for normal ready verification, semantically check that each authored AC/Verification flow can decide its approved claim, apply mode-specific Ready runtime admission to bind the immutable target before product/runtime work, obtain discriminating verifier-owned evidence without project-local remediation, require SUBAGENT PRE_RUNTIME and VERIFIED-only PRE_PROGRESSION Parent continuation plus event-driven material-turn checkpoints, never silently fall back between modes, adjudicate every authored flow and AC, and close the verdict/status transition through ready_guard finalize_verification."
