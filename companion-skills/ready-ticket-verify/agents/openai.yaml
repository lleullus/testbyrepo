interface:
  display_name: "Ready Ticket Verify"
  short_description: "Verify one Ready Ticket with direct-first checkpointed delegation"
  default_prompt: "Use $ready-ticket-verify in its default DIRECT mode to verify the exact Ready Ticket, require a current COMPLETE ready-ticket heuristic-probe handoff plus its exact Probe Machine Binding for normal ready verification, semantically check that each authored AC/Verification flow can decide its approved claim, bind the immutable verification target with ready_guard begin_verify before product/runtime work, obtain discriminating verifier-owned evidence through the guarded runtime path without project-local remediation, use exactly one checkpointed verifier only when I explicitly select SUBAGENT for this stage, require PRE_RUNTIME and VERIFIED-only PRE_PROGRESSION Parent continuation plus event-driven material-turn checkpoints, adjudicate every authored flow and AC, and close the verdict/status transition through ready_guard finalize_verification."
