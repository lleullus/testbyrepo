interface:
  display_name: "Ready Ticket Verify"
  short_description: "Verify one Ready Ticket with direct-first checkpointed delegation"
  default_prompt: "Use $ready-ticket-verify in its default DIRECT mode to verify the exact Ready Ticket, require a current COMPLETE ready-ticket heuristic-probe handoff for normal ready verification, semantically check that each authored AC/Verification flow can decide its approved claim, use exactly one checkpointed verifier only when I explicitly select SUBAGENT for this stage, require PRE_RUNTIME and VERIFIED-only PRE_PROGRESSION Parent continuation plus event-driven material-turn checkpoints, obtain fresh verifier-owned Zero-Mock evidence from actual production/dependency/readback paths for every authored Verification flow, adjudicate every AC, and leave the guarded done transition owned by that verifier."
