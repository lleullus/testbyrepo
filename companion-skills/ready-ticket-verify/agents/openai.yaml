interface:
  display_name: "Ready Ticket Verify"
  short_description: "Verify one Ready Ticket through one worker"
  default_prompt: "Use $ready-ticket-verify to assign the exact Ready Ticket to one verifier worker by default, execute every authored Verification flow on a stable target, adjudicate every AC, and own the guarded done transition."
