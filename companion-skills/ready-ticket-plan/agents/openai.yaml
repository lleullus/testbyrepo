interface:
  display_name: "Ready Ticket Plan"
  short_description: "Prepare and independently review exact Ready Ticket execution methods"
  default_prompt: "Use $ready-ticket-plan for the exact Ready Tickets: preserve product authority, reuse current evidence, prepare only relevant shared method decisions with grounded Code Grounding, obtain an independent Plan Review invocation separate from the writer that reads the original contract and current primary evidence directly, preserve my current execution-mode and model choices, and return READY TICKET PLAN RESULT with the actual outside-root iis-plan-review/v1 artifact. Do not manufacture ADMIT from a plan file or JSON shape, implement source, or issue final verification verdicts."
