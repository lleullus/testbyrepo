interface:
  display_name: "Scope Plan"
  short_description: "Prepare a grounded method and Assurance Baseline"
  default_prompt: "Use $scope-plan for the exact ready Scope. Read bound originals and current implementation directly. Write the method and machine-readable Assurance Baseline, connect discriminators to existing project checks or exact observation limits, preserve conditional first work and accessible execution handoff, and validate source/structure. Return SCOPE PLAN RESULT without implementing, changing originals, dispatching an approval role or claiming delivery completion."
