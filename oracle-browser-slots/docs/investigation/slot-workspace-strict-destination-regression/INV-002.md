# Slot Workspace Project Composer Failure Follow-up Repository Investigation

Artifact-Type: repository-investigation
Format-Version: 1
Status: complete
Project-Root: /home/user01/project/oracle/oracle-browser-slots
Repository-Root: /home/user01/project/oracle
Investigation-Slug: slot-workspace-strict-destination-regression
Investigation-Revision: INV-002
Observed-At: 2026-09-10T10:54:03+09:00
Git-Branch: main
Git-Head: d6d548b8c5c13cde43e6ba40f818128b9e7b84aa
Working-Tree-Before-Artifact: dirty
Evidence-Authority: repository evidence only; not IIS planning authority

## Evidence Handoff

### Investigation Answer

Answer: The mapped slot reaches the exact, valid ChatGPT project URL and has an authenticated session, but the deployed ChatGPT project route renders its `Try again` error gate instead of the project conversation shell. The deployed route bundle binds that gate to a required `/backend-api/locked_chats/status` query; the current authenticated runtime returns HTTP 404 for that endpoint while other authenticated endpoints return 200. With no app shell or composer in the resulting document, the wrapper correctly fails before stock Oracle starts. This is not an `about:blank` race, URL mapping error, login failure, or composer selector miss. No inspected local wrapper or stock Oracle path can safely bypass the failed project gate while preserving project identity; treating the locked-chat query as successful would bypass an external security/privacy control.
Based On: Finding 1, Finding 2, Finding 3, Finding 4, Finding 5, Finding 6, Finding 7, Finding 8, Finding 9, Finding 10, Inference 1, Inference 2, Inference 3, Inference 4

### Observed Current Product State

- State: All managed slots were freshly restarted and broadly prepared. Slots 3, 4, and 5 reach their exact mapped `/project` URLs but render only `Try again` with zero composer candidates. Slot 4 strict readback is authenticated and page-ready but `composer_ready=false`; its actual wrapper run ends `pre_submit_failed`, `child_started=false`. A completed 2026-09-09 session proves the same mapping previously submitted and transitioned to a project-scoped `/c/<id>` URL.
  Supporting Findings: Finding 1, Finding 2, Finding 5, Finding 6, Finding 8

### Existing Product Surfaces And Readbacks

- Trigger: submission-capable mapped wrapper run plus read-only CDP/runtime and persisted-session inspection.
  Observable surface: wrapper lifecycle, exact target metadata/runtime DOM/authenticated endpoint responses, and stock session `meta.json`.
  Authoritative readback: current `pre_submit_failed`, `child_started=false`, exact `/project`, body `Try again`, zero composer candidates, locked-chats status HTTP 404; historical success records `promptSubmitted=true`, a stable project-scoped conversation URL/ID, completed result, and released slot.
  Supporting Findings: Finding 1, Finding 2, Finding 3, Finding 4, Finding 5, Finding 6, Finding 8

### State And Authority

- Statement: Settings owns the requested mapped destination; slot JSON owns broad preparation, occupancy, process/profile identity, and lifecycle status; strict CDP owns immediate exact target readiness; stock session metadata owns the resulting stable conversation identity. The wrapper closes only strict targets it creates and never treats broad prepared status as exact project composer evidence.
  Supporting Findings: Finding 6, Finding 7, Finding 8, Finding 9

### Planning Constraint Candidates

- Preserve fail-closed behavior: never accept the root ChatGPT composer as success for a failed mapped project URL.
  Supporting Findings: Finding 5, Finding 6
- Never synthesize or intercept a successful `/locked_chats/status` response; that endpoint is part of a deployed locked-chat gate and bypassing it would cross an external security/privacy authority boundary.
  Supporting Findings: Finding 3, Finding 4, Finding 5
- Any automatic retry action must remain non-submitting, bounded, exact-target checked after the action, and must not close an existing user/launcher target.
  Supporting Findings: Finding 2, Finding 6, Inference 3
- Runtime success must be proven by an actual submission-capable mapped run and resulting project-scoped conversation/result, not by unit fakes, `prepare`, `status`, `--dry-run`, or `--preview`.
  Supporting Findings: Finding 8, Finding 10, Inference 4

### Product Dependency Candidates

- A functioning ChatGPT project route whose required authenticated project-gate queries succeed and whose page exposes a usable composer.
  Supporting Findings: Finding 2, Finding 3, Finding 4, Finding 5
- Stable project-scoped conversation URL capture after the first prompt submission.
  Supporting Findings: Finding 8, Finding 9

### Material Unknowns

- None

### Load-Bearing Anchors

- A1 | SOURCE | oracle-browser-slots/oracle_browser_slots/cdp.py | `CDPClient.ensure_strict_destination`, `_STRICT_DESTINATION_READBACK_SCRIPT` | sha256 b5f1c1c10e0f20f7f126165384dce0c21f435dfc4bc389410ea69ca2ff070289 | ANCHOR_LOCAL
- A2 | SOURCE | oracle-browser-slots/oracle_browser_slots/runner.py | `execute_claimed`, `_prepare_strict_destination`, `_validated_oracle_command` | sha256 5fb57b86a6295aa2c848c395fb6143e3898a39c3340c809c5d09151624c7adc3 | ANCHOR_LOCAL
- A3 | SOURCE | src/browser/index.ts | local and remote browser-tab/navigation paths | sha256 44416254e6801504226e4df9980aaeb864f69b6ff609907367c34881ae310e29 | ANCHOR_LOCAL
- A4 | SOURCE | src/browser/actions/navigation.ts | `navigateToPromptReadyWithFallback`, `ensurePromptReady`, blocking UI handling | sha256 f62bf206c6a5479079f2fc8d5e803939f5fd7f7557b65b742cccb69571f9d9d6 | ANCHOR_LOCAL
- A5 | SOURCE | src/browser/liveTabs.ts | exact URL tab resolution and target attachment | sha256 300d11c632c84512edf069172b44053d854e44364d145c51a373e57533e3402b | ANCHOR_LOCAL
- A6 | DOC | oracle-browser-slots/README.md | workspace URL rules 226-272 | sha256 2790b39a8fd3cb2e9714dca413e1eb7697cdfbcbd2bfa515d712a88fd4e38971 | ANCHOR_LOCAL
- A7 | TEST | oracle-browser-slots/tests/test_slots.py | `CDPStrictDestinationTests` | sha256 b75efaf0fa98e250505b6bd6c6d5d1e6ff36f59ff068149c7a7120c4ba903906 | ANCHOR_LOCAL
- A8 | TEST | oracle-browser-slots/tests/test_workspace_mapping.py | mapping, strict pin, child-boundary fakes | sha256 71f2285ae5ed5c733c9a29878530837f365164a4105fcaab2c917e8a537856dd | ANCHOR_LOCAL
- A9 | RUNTIME | slot 4 CDP target | exact URL, `Try again`, zero composer candidates, strict readback fields | slot 4 port 19225, current restarted Chrome | RUNTIME_STATE
- A10 | RUNTIME | slots 1/3/4/5/10 CDP comparison | roots 1/10 healthy; mapped projects 3/4/5 `Try again` and composer count 0 | current restarted Chrome set | RUNTIME_STATE
- A11 | DOC | `https://chatgpt.com/cdn/assets/g._gizmoSlug.project-c24gd5ne.js` | `queryFn: O`; `isError` renders `chatgpt.locked_chats.project_gate.retry` / `Try again` | retrieved 2026-09-10 | EXTERNAL_VERSION
- A12 | DOC | `https://chatgpt.com/cdn/assets/4813494d-ilaxclpwvg5i0e40.js` | `oFe -> safeGet('/locked_chats/status',{authOption:Required})`; export `oFe as lJ`; route imports `lJ as O` | retrieved 2026-09-10 | EXTERNAL_VERSION
- A13 | RUNTIME | authenticated page-context GET | `/backend-api/locked_chats/status` 404 `{"detail":"Not found"}`; `/backend-api/settings/user` 200; token not emitted | slot 4 current page | RUNTIME_STATE
- A14 | RUNTIME | `~/.oracle/sessions/gemini-3-8-flash-luna/meta.json` | project browserTabRef/config URL; prompt submitted; project-scoped `/c/<id>` runtime URL; completed | 2026-09-09 persisted session | RUNTIME_STATE
- A15 | SEARCH | wrapper/stock project navigation, reattach, followup, project sources, related tests/docs | project new-chat URL/API/helper search | HEAD/current working tree | SEARCH_UNIVERSE
- A16 | RUNTIME | `~/.oracle/browser-slots/slot-4.json` | prepared true, requires_reprepare false, occupancy null, broad status usable | current restarted slot state | RUNTIME_STATE
- A17 | SOURCE | src/browser/conversationUrl.ts | stable `/c/<id>` extraction at any path position | Git HEAD d6d548b8c5c13cde43e6ba40f818128b9e7b84aa | ANCHOR_LOCAL
- A18 | SOURCE | oracle-browser-slots/oracle_browser_slots/followup.py | project-prefix-preserving conversation identity parser | Git HEAD d6d548b8c5c13cde43e6ba40f818128b9e7b84aa | ANCHOR_LOCAL

## Investigation Charter

### Original Request

Run a follow-up investigation with lanes selected after Main reconnaissance, assign two GPT-5.6 Luna Max workers per lane in parallel, synthesize the completed evidence into a new follow-up work plan, then have a Sol XHigh worker execute that plan under Main communication and audit.

### Investigation Objective

Determine why a freshly prepared exact mapped ChatGPT `/project` target has no usable composer, identify the actual local/external failure boundary, and bound which remediation paths can preserve exact workspace and target-ownership guarantees.

### Decision Context

The earlier `about:blank` retry correction passed its tests but the user-visible outcome remains unavailable. A later plan must fix the actual end-to-end boundary or explicitly refuse an unsafe/ineffective local workaround.

### Explicit Exclusions

- During investigation: no source mutation, UI click, navigation, reload, target creation/closure, prompt submission, or test execution by investigation workers.
- No interception, fabrication, or bypass of ChatGPT authentication, project membership, locked-chat status, or other external security/privacy controls.
- No root-ChatGPT fallback counted as project workspace success.

## Repository Binding

Relevant Modified Paths:
- `oracle-browser-slots/oracle_browser_slots/cdp.py` — Main's earlier `about:blank` grace change.
- `oracle-browser-slots/tests/test_slots.py` — Main's earlier regression tests.

Relevant Untracked Paths:
- `oracle-browser-slots/docs/investigation/` — INV-001 and this follow-up artifact workspace.

Connected Repository Surface:
- wrapper CLI/runner/CDP/config/state/launcher/followup;
- stock Oracle remote/local target selection, navigation, prompt readiness, provider submission, conversation URL capture;
- wrapper and stock tests/docs;
- current managed-slot CDP state;
- deployed ChatGPT project route bundles and authenticated read-only endpoint results.

## Investigation Frontier

| Area | Question | Disposition | Result |
| --- | --- | --- | --- |
| FLOW_AND_READBACK | What exact flow stops before child start, and what upstream runtime condition produces it? | INVESTIGATE | Exact project target is authenticated and loaded, but the project route error gate has no composer; strict preflight stops before Popen. |
| STATE_AND_AUTHORITY | Which URL, target, slot, project, and conversation identities are authoritative at each phase? | INVESTIGATE | Mapping governs requested workspace; slot state governs lifecycle; strict CDP governs pre-child target readiness; stock `/c/<id>` governs durable conversation identity. |
| ALTERNATE_PATHS | Can stock navigation, fallback, followup, reload, or another existing route safely recover? | INVESTIGATE | Root fallback violates workspace scope; unpinned remote fallback can select unintended targets; followup only resumes known conversations; no project-new-chat route was found. |
| EVIDENCE_ALIGNMENT | Why did green tests not prove the actual product outcome? | INVESTIGATE | Tests inject ready dictionaries and fake children; they prove state-machine/wiring branches but never execute the deployed project gate or a real mapped submission. |

Each frontier area received two independent GPT-5.6 Luna Max first passes with gated peer evidence exchange. Main directly re-opened all load-bearing source/runtime anchors and resolved differences from primary evidence.

## Current System Model

### Entry And Execution Flow

`run/submit -> validate/inject mapped URL and slot remote endpoint -> claim slot -> ensure_strict_destination -> exact target metadata and runtime readback -> inject exact --browser-tab -> Popen stock Oracle -> stock attach -> prompt readiness -> submit -> capture project conversation URL`.

Current execution stops at `ensure_strict_destination`: target URL, document readiness, and authenticated session pass; the deployed project route error gate yields no composer, so `Popen` is never called.

### State, Data And Lifecycle Ownership

- `Settings`: configured mapped destination and caller precedence.
- Slot JSON: broad prepared/reprepare/occupancy/process lifecycle only.
- CDP strict preflight: exact live target URL, auth, page, and usable composer immediately before child.
- Stock Oracle session metadata: post-submit target, stable conversation ID/URL, prompt submission and completion.
- ChatGPT backend/frontend: project route, project membership, locked-chat gate, and composer availability.

### Configuration And Extension Boundaries

- Caller `--chatgpt-url`/`--browser-url` overrides slot mapping.
- Exact `--browser-tab` is an existing-target reference. Stock skips initial navigation when attached.
- Without a pin, stock remote mode creates/navigates a target but may fall back to an unintended first target after target-creation failure.
- Stock local fallback can submit at `https://chatgpt.com/` after a project URL fails; that is not acceptable workspace success.
- Followup deliberately bypasses mapping/strict preparation only for a known stable parent conversation.

### Observable Results And Readbacks

- Mapping success alone: exact `/json/list` URL.
- Pre-child readiness: exact runtime URL + loaded page + authenticated session + visible enabled composer.
- Current project error: body/control `Try again`, app shell absent, locked-chats status endpoint 404.
- End-to-end success: actual child start, prompt submitted, stable project-scoped `/c/<id>`, completed answer, released clean slot.

## Verified Findings

### Finding 1 — Current target reaches the exact mapped URL

Classification: FACT
Statement: Slot 4 has one exact page target for the configured project URL; there is no current blank, redirect, or duplicate condition.
Primary Evidence: A9
Counterevidence Checked: target metadata and runtime `location.href` both match.
Boundary / Limitation: Current restarted runtime snapshot only.
Planning Relevance Candidate: CURRENT_STATE
Freshness Sensitivity: RUNTIME_STATE

### Finding 2 — The exact page is an error gate, not a composer selector miss

Classification: FACT
Statement: The document is complete and authenticated but contains only a visible `Try again` button, with no app shell, form, textarea, contenteditable, role=textbox, `.ProseMirror`, or other composer candidate.
Primary Evidence: A9, A10
Counterevidence Checked: healthy root slots expose the expected app shell and composer using the same probe.
Boundary / Limitation: The external UI can change after this observation.
Planning Relevance Candidate: CURRENT_STATE
Freshness Sensitivity: RUNTIME_STATE

### Finding 3 — The deployed project route owns the Try again surface

Classification: FACT
Statement: The loaded project route bundle renders the exact `Try again` message only when its project gate query is in `isError` state.
Primary Evidence: A11
Counterevidence Checked: route import alias and shared-bundle export mapping were traced rather than inferring from button text.
Boundary / Limitation: Bound to the named deployed bundle version.
Planning Relevance Candidate: PRODUCT_DEPENDENCY_CANDIDATE
Freshness Sensitivity: EXTERNAL_VERSION

### Finding 4 — The failed project gate query is locked-chat status

Classification: FACT
Statement: The project route query function resolves to an authenticated GET of `/locked_chats/status`.
Primary Evidence: A11, A12
Counterevidence Checked: `lJ as O`, `oFe as lJ`, and the `oFe` implementation were matched across deployed bundles.
Boundary / Limitation: Bound to the named deployed assets.
Planning Relevance Candidate: PRODUCT_DEPENDENCY_CANDIDATE
Freshness Sensitivity: EXTERNAL_VERSION

### Finding 5 — Current authenticated locked-chat status returns 404

Classification: FACT
Statement: The slot 4 page context has an access token and receives HTTP 404 `Not found` from `/backend-api/locked_chats/status`; an authenticated settings endpoint returns 200. Slots 3, 4, and 5 show the same project error/composer result.
Primary Evidence: A10, A13
Counterevidence Checked: valid mapping/project sidebar identity and authenticated control endpoints; healthy root pages remain usable.
Boundary / Limitation: Server-side reason and future recovery are not locally observable.
Planning Relevance Candidate: PRODUCT_DEPENDENCY_CANDIDATE
Freshness Sensitivity: RUNTIME_STATE

### Finding 6 — Wrapper failure is correctly before child submission

Classification: FACT
Statement: Strict destination requires page, login, and composer after exact URL identity; runner calls this before Popen and returns `pre_submit_failed`, `child_started=false` on failure.
Primary Evidence: A1, A2, A9
Counterevidence Checked: stock prompt readiness would also fail if reached because the same document has no composer.
Boundary / Limitation: Does not establish server-side root reason.
Planning Relevance Candidate: PLANNING_CONSTRAINT_CANDIDATE
Freshness Sensitivity: ANCHOR_LOCAL

### Finding 7 — Broad prepared status is not exact project readiness

Classification: FACT
Statement: Slot state can remain `사용 가능` because prepare/status verifies broad ChatGPT login, while strict mapped execution separately verifies exact project composer readiness.
Primary Evidence: A1, A2, A16
Counterevidence Checked: exact strict readback remains false while occupancy is clear.
Boundary / Limitation: Operator wording may still be misleading, but the authorities are distinct.
Planning Relevance Candidate: CURRENT_STATE
Freshness Sensitivity: ANCHOR_LOCAL

### Finding 8 — The exact project flow previously worked and changes URL after submit

Classification: FACT
Statement: A 2026-09-09 completed stock session used the same exact project `browserTabRef`, submitted a prompt, and recorded a stable project-scoped conversation URL `/g/<project>/c/<id>`.
Primary Evidence: A14
Counterevidence Checked: completed status, promptSubmitted, target ID, conversation ID, archive URL, config URL all agree.
Boundary / Limitation: Historical counterexample, not current readiness.
Planning Relevance Candidate: DELIVERY_CONTEXT
Freshness Sensitivity: RUNTIME_STATE

### Finding 9 — Project landing and durable conversation are distinct identities

Classification: FACT
Statement: The mapped project landing selects the workspace before submission; the resulting stable `/c/<id>` is the durable conversation identity. Stock accepts `/c/<id>` at any path position, while wrapper archived restore currently accepts only root `/c/<id>`.
Primary Evidence: A14, A17, A18, A1
Counterevidence Checked: wrapper tests contain no project-scoped conversation URL restore case.
Boundary / Limitation: Restore incompatibility is adjacent and not the cause of current pre-child failure.
Planning Relevance Candidate: DELIVERY_CONTEXT
Freshness Sensitivity: ANCHOR_LOCAL

### Finding 10 — Current tests cannot prove deployed project readiness

Classification: FACT
Statement: Wrapper strict tests return prerecorded runtime dictionaries, mapping tests use an unconditional FakeCDP success and fake child, and sessionless tests deliberately bypass strict preparation. Green tests prove local branches but not live project-gate or end-to-end mapped submission behavior.
Primary Evidence: A7, A8
Counterevidence Checked: existing tests still materially cover exact URL, retry, redirect, duplicate, cleanup, injection, and no-child failure branches.
Boundary / Limitation: This is a bounded claim about the inspected tests, not all possible external validation.
Planning Relevance Candidate: ACCEPTANCE_SURFACE_CANDIDATE
Freshness Sensitivity: ANCHOR_LOCAL

## Inferences

### Inference 1 — Strongest current root-cause model

Statement: The immediate upstream cause of `composer_ready=false` is the deployed ChatGPT project route's failed locked-chat-status query, not local URL routing, authentication, or selector logic.
Supported By: Finding 2, Finding 3, Finding 4, Finding 5
Plausible Alternative: Another project-specific query could fail simultaneously, but the route bundle explicitly maps the observed gate to locked-chat status and the required endpoint currently returns 404.
Evidence Needed To Disprove: Current deployed route code or response trace showing a different query produced the rendered gate.
Planning Relevance Candidate: PRODUCT_DEPENDENCY_CANDIDATE

### Inference 2 — Local bypass is unsafe

Statement: Fabricating success for the failed locked-chat query, ignoring the project gate, or falling back to the root composer would either bypass an external privacy/security control or submit outside the requested workspace.
Supported By: Finding 3, Finding 4, Finding 5, Finding 6
Plausible Alternative: A documented ChatGPT-supported project-new-chat route could avoid the gate.
Evidence Needed To Disprove: A current supported route/API with authoritative project binding and composer readback.
Planning Relevance Candidate: PLANNING_CONSTRAINT_CANDIDATE

### Inference 3 — Bounded retry can improve recovery but cannot guarantee current success

Statement: A non-submitting, bounded `Try again`/reload recovery followed by exact URL and composer revalidation could safely handle a transient external error only if its action is narrowly identified and target ownership is respected; it cannot repair a persistent authenticated 404.
Supported By: Finding 2, Finding 3, Finding 4, Finding 5, Finding 6
Plausible Alternative: The query could auto-retry and recover without an explicit action, but the observed page remained at the error gate through the current strict deadline.
Evidence Needed To Disprove: Authorized action trace showing deterministic recovery or proving the action changes project identity.
Planning Relevance Candidate: DELIVERY_CONTEXT

### Inference 4 — End-to-end acceptance is mandatory

Statement: No later implementation is complete until a real mapped submission-capable run starts stock Oracle, submits exactly once in the intended project, records a stable project-scoped conversation URL/result, cleans wrapper-owned targets, and releases the slot.
Supported By: Finding 6, Finding 8, Finding 10
Plausible Alternative: None for the requested user-visible outcome.
Evidence Needed To Disprove: Not applicable.
Planning Relevance Candidate: ACCEPTANCE_SURFACE_CANDIDATE

## Alternate, Legacy And Bypass Paths

- Remove exact `--browser-tab`: stock remote mode may create/navigate a target but can fall back to an unintended first tab after target creation failure; it does not solve the external project gate.
- Stock local prompt fallback: reaches root ChatGPT after project prompt failure and can submit outside the mapped workspace; rejected as false completion.
- Wrapper followup: safely resumes a known stable conversation in its original slot but cannot create the first project conversation from a project shell.
- Arbitrary existing `/c` tab reuse: lacks automatic project/lineage authority and can append to the wrong conversation.
- Project Sources flow: separate local-only source management, not a prompt submission route.
- Sessionless controls: bypass strict preparation by design and cannot prove submission behavior.
- Generic `Try again` click/reload: no stable repository contract; only potentially acceptable as a bounded recovery experiment with post-action exact readback, never as success itself.

## Documentation, Tests And Runtime Alignment

- README, wrapper source, and current runtime agree that a not-ready project target fails before child start.
- README's broad `사용 가능` status and strict execution failure are separate authorities, though the operator-facing distinction is easy to misread.
- Tests align with local branch semantics but do not execute the current deployed project route; the earlier false completion came from promoting synthetic tests to end-to-end proof.
- Stock local fallback behavior conflicts with exact workspace success if adopted as a remediation.
- Project-scoped conversation URLs exist in stock runtime evidence, but wrapper archived-restore canonicalization and tests currently assume root `/c/<id>` only; this is an adjacent compatibility gap.

## Absence Claims

### Absence Claim 1

Claim: No safe repository-backed project-new-chat URL/API/helper independent of the `/project` route was found.
Search Universe: wrapper runner/CDP/followup/launcher/model/service/tests/docs; stock browser index/navigation/liveTabs/chromeLifecycle/reattach/reattachHelpers/projectSources/followup/conversation URL sources and connected tests/docs.
Method: bounded searches for new chat/new conversation route and project `/g/`, `/project`, `/c/`, navigation, browserTabRef, resumeConversationUrl constructs, followed by source inspection.
Result: not found within the bounded universe; found only project landing, existing conversation recovery, project sources, and generic root fallback.
Boundary: Undocumented external ChatGPT routes and server source were not available.

### Absence Claim 2

Claim: No wrapper test executes strict readback JavaScript against a live project DOM and then starts a real mapped child.
Search Universe: `oracle-browser-slots/tests/test_slots.py`, `test_workspace_mapping.py`, and connected wrapper tests.
Method: inspect FakeCDP/FakeWebSocket/child seams and assertions.
Result: not found within the bounded test universe.
Boundary: Actual runtime probes are separate evidence, not permanent tests.

## Contradictions And Surprises

- The first follow-up model treated `composer_ready=false` as possibly a selector/readiness issue. Direct DOM comparison and deployed bundle tracing show an explicit project error gate.
- All mapped project slots fail together while root slots remain healthy, which contradicts a slot-4-only profile or mapping explanation.
- The project route requires a locked-chat status endpoint even for this account, but that authenticated endpoint currently returns 404; the local repository does not own that external contract.
- The same exact project attachment completed successfully one day earlier, so the current failure is not a timeless incompatibility with exact project URLs.
- Stock local fallback can produce a successful answer outside the project, making apparent success a workspace-correctness failure.

## Coverage Boundary

Inspected:
- All four mandatory investigation areas with two independent Luna Max workers per area.
- Wrapper execution, strict CDP, mapping/config, lifecycle/state, target cleanup, followup.
- Stock remote/local attach/navigation, prompt readiness, provider submission, conversation URL capture, project helpers.
- Relevant wrapper/stock tests and documentation.
- Current fresh managed-slot runtime, exact project DOM/auth state, project mapping identity, deployed project gate bundles, required endpoint status.
- Historical successful same-project stock session.

Not Inspected:
- ChatGPT backend server logs, deployment configuration, or source.
- Mutation-based `Try again` click/reload recovery during investigation.
- Undocumented external project-new-chat APIs.
- Unrelated repository subsystems and user-owned `tmp/`.

Why Current Coverage Is Sufficient Or Insufficient:
The current failure boundary, responsible external query, local authority boundary, unsafe bypasses, historical counterexample, and acceptance evidence are established. The unresolved server-side reason for the authenticated 404 may affect when the external service recovers, but it does not change the conclusion that local code cannot safely fabricate project-gate success. Additional local source inspection cannot produce missing external authority.

## Completion

Investigation Completion: COMPLETE
Decision-Critical Unknowns: None
