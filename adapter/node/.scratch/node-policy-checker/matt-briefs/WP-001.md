# WP-001: 자동 변경 입장 심사

Status: ready-for-matt
Parent-Project-Map: ../PROJECT-MAP.md
Work-Package: WP-001
Project-Root: /home/user01/project/iis-skills/adapter/node
Suggested-Work-Slug: node-policy-checker-wp-001-change-admission

## Authority Notice

이 brief는 승인된 프로젝트 분해를 투영하며 첫 Matt 대화의 활성 package 범위를 고정한다. 승인된 package Spec이 아니고 상세 요구사항을 조용히 추가하지 않는다. Matt는 `to-spec` 전에 계약 범위의 공유 이해를 사용자에게 확인받아야 한다.

## Product Context

사용자는 코딩 구조를 직접 설계할 수 없으므로, LLM이 코드를 쓰기 전에 기존 구조와 재사용 가능성을 실제 근거로 확인하고 성급한 변경을 멈추게 할 독립적인 입장 심사가 필요하다.

## Package Outcome

비개발 사용자와 LLM 운영자는 수동 아키텍처 설정 없이 변경 전에 기존 실행 경로, 책임 소유자, 재사용 후보와 위험을 근거로 확인하고, 성급한 쓰기를 차단하는 자동 입장 판정을 받을 수 있다.

## Included Product Scope

- a-1의 모듈 및 레이어 경계, a-2의 기계적 의존성 정책, a-3의 SSOT, a-4의 디렉터리 위계를 변경 전 저장소에서 자동 발견하고 근거와 함께 고정한다.
- b-4의 빈 입력, null, 경계값, 동시성, 실패 경로, side effect 테스트 의무를 코드 작성 전에 도출한다.
- b-5의 새 function, helper, type, shape 생성 전에 기존 재사용 후보를 검색하고 생성 필요성을 판정한다.
- 사용자는 기술 구조 대신 원하는 행동, 보존할 행동, 실패 시 관측 결과만 제공한다.
- 필수 근거가 완전할 때만 Write Permit을 발급하고 판단 불가는 `INCONCLUSIVE`로 차단한다.

## Excluded Sibling Scope

- 작성된 코드의 정적 및 AI 의미 품질 판정은 WP-002가 소유한다.
- 테스트 실행, 변이, edge case 및 side effect 증명은 WP-003이 소유한다.
- LLM 수정 반복과 CI 결과 소비 lifecycle은 WP-004가 소유한다.

## Dependencies

None

## Adopted Initiative Decisions

- 사용자는 레이어, SSOT, 에러 경계, mock 경계를 직접 선언하지 않고 원하는 행동과 보존 및 실패 결과만 설명한다.
- 프로젝트 고유 구조는 변경 전 저장소와 승인된 제품 의도에서 자동 추론하고 코드 쓰기 전에 고정한다.
- 근거 없는 LLM 자기 진술은 입장 증거가 아니며 판단 불가는 통과시키지 않는다.

## Decisions Reserved For Matt

- 현재 상태 검사와 변경 세션에서 각각 어떤 최소 근거가 입장 판정을 완성하는지 정한다.
- 기술 지식이 없는 사용자에게 보여줄 입장 요약과 필요한 행동 질문의 범위를 정한다.
- 자동 허용, 차단, 판단 불가의 사용자 관측 의미와 다시 시도하는 조건을 정한다.

## Reference Material

- 승인된 상위 Project Map: ../PROJECT-MAP.md

## Matt Start

이 Work Package만을 계획한다. 먼저 Outcome, Included Product Scope, Excluded Sibling Scope를 다시 말하고 사용자가 현재 대화에서 이미 승인한 frame이 정확한지 확인하거나 수정한다. 그다음 확인된 package 범위 안에서 정상 `ask-matt` 흐름을 진행한다. sibling 범위를 흡수해야 한다면 Project Shaper로 shaping delta를 반환한다.
