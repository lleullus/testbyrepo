# WP-002: 자동 코드 및 리뷰 품질 판정

Status: ready-for-matt
Parent-Project-Map: ../PROJECT-MAP.md
Work-Package: WP-002
Project-Root: /home/user01/project/iis-skills/adapter/node
Suggested-Work-Slug: node-policy-checker-wp-002-code-review-gate

## Authority Notice

이 brief는 승인된 프로젝트 분해를 투영하며 첫 Matt 대화의 활성 package 범위를 고정한다. 승인된 package Spec이 아니고 상세 요구사항을 조용히 추가하지 않는다. Matt는 `to-spec` 전에 계약 범위의 공유 이해를 사용자에게 확인받아야 한다.

## Product Context

변경 전 입장 심사를 통과했더라도 작성 결과에는 에러 삼킴, 중복, 숨은 결합, 경계 위반과 과도한 복잡성이 생길 수 있으므로 완료 전에 자동 품질 판정이 필요하다.

## Package Outcome

LLM이 작성한 Node/TypeScript 변경은 WP-001에서 고정한 근거와 정책에 대해 코드 및 리뷰 품질을 자동 판정받고, 모든 요청 검사를 통과하기 전에는 완료로 취급되지 않는다.

## Included Product Scope

- b-1의 에러 삼킴과 무의미한 fallback, b-2의 과한 중첩, b-3의 함수 및 파일 크기 상한을 판정한다.
- c-1의 중복, 대체 누락, 죽은 코드, c-2의 조용한 에러 삼킴, c-3의 숨은 결합과 암묵 계약, c-4의 re-export, c-5의 fan-in 및 fan-out 집중을 판정한다.
- d-1의 strict 타입, 금지된 type assertion과 JavaScript JSDoc 의무, d-2의 순환 의존성과 경계 위반, d-3의 complexity, depth, 함수 크기 제한을 강제한다.
- 확정 가능한 항목은 기계 판정하고 의도와 의미가 필요한 항목은 근거가 연결된 AI 판정을 사용한다.
- 요청된 검사 하나라도 실행 또는 판정하지 못하면 `INCONCLUSIVE`로 실패하며, 변경된 코드로 정책을 다시 만들어 위반을 합법화하지 못한다.

## Excluded Sibling Scope

- 변경 전 Write Permit과 정책 고정은 WP-001이 소유한다.
- 테스트가 행동, edge case, 실패와 side effect를 실제로 검출하는지에 대한 실행 증명은 WP-003이 소유한다.
- 실패 결과를 소비해 LLM이 수정하고 CI가 최종 결과를 확인하는 lifecycle은 WP-004가 소유한다.

## Dependencies

- WP-001 — 변경 후 판정이 자기합법화되지 않으려면 변경 전에 고정된 입장 근거와 정책이 먼저 존재해야 한다.

## Adopted Initiative Decisions

- 승인된 checklist의 b-1~b-3, c-1~c-5, d-1~d-3은 모두 자동 판정 범위다.
- 확정 규칙은 기계 검사, 의미 규칙은 근거 기반 AI 검사를 사용하며 어떤 필수 판정 누락도 성공으로 정규화하지 않는다.
- 결과는 `PASS`, `FAIL`, `INCONCLUSIVE`를 구분하고 `INCONCLUSIVE`도 완료를 차단한다.

## Decisions Reserved For Matt

- 사용자가 선택할 검사 깊이별 의무와 각 깊이에서 허용되지 않는 미검사 상태를 정한다.
- 기존 저장소의 선행 위반과 이번 변경에서 생긴 위반을 사용자에게 어떻게 구분하고 차단할지 정한다.
- AI 의미 판정의 근거 표시, 반증 가능성, 판정 충돌을 사용자에게 어떻게 보여줄지 정한다.

## Reference Material

- 승인된 상위 Project Map: ../PROJECT-MAP.md

## Matt Start

이 Work Package만을 계획한다. 먼저 Outcome, Included Product Scope, Excluded Sibling Scope를 다시 말하고 사용자가 현재 대화에서 이미 승인한 frame이 정확한지 확인하거나 수정한다. 그다음 확인된 package 범위 안에서 정상 `ask-matt` 흐름을 진행한다. sibling 범위를 흡수해야 한다면 Project Shaper로 shaping delta를 반환한다.
