# WP-003: 실행 및 변이 기반 행동 증명

Status: ready-for-matt
Parent-Project-Map: ../PROJECT-MAP.md
Work-Package: WP-003
Project-Root: /home/user01/project/iis-skills/adapter/node
Suggested-Work-Slug: node-policy-checker-wp-003-behavior-proof

## Authority Notice

이 brief는 승인된 프로젝트 분해를 투영하며 첫 Matt 대화의 활성 package 범위를 고정한다. 승인된 package Spec이 아니고 상세 요구사항을 조용히 추가하지 않는다. Matt는 `to-spec` 전에 계약 범위의 공유 이해를 사용자에게 확인받아야 한다.

## Product Context

정적 품질이 깨끗해도 테스트가 문자열이나 내부 mock만 확인하면 실제 실패와 side effect 회귀를 놓칠 수 있으므로, 테스트 실효성을 실행 결과로 독립 증명해야 한다.

## Package Outcome

사용자와 LLM은 변경된 행동, edge case, 실패 경로와 side effect가 테스트로 실제 검출되며 내부 구현 mock이나 테스트용 방어 코드가 품질을 가장하지 않는다는 자동 증명을 받을 수 있다.

## Included Product Scope

- e-1의 문자열 또는 커버리지 수치가 아닌 관측 가능한 출력 행동 검증 여부를 실행 및 변이 결과로 판정한다.
- e-2의 mock이 자동 추론된 외부 경계에서만 사용되는지 판정하고 내부 구현 세부 mock을 차단한다.
- e-3의 빈 입력, 경계값, null, 실패 경로와 관련 edge case를 도출하고 테스트로 고정되었는지 실행 확인한다.
- e-4의 테스트 통과만을 위해 production code에 방어 코드가 추가되었는지 변경분, 실행 결과, 변이 생존 여부를 함께 판정한다.
- 테스트 실행 능력이나 필수 행동 증거가 부족하면 성공 대신 `INCONCLUSIVE`로 실패한다.

## Excluded Sibling Scope

- 변경 전 구조 및 테스트 의무 도출은 WP-001이 소유한다.
- source 및 diff의 코드와 리뷰 품질 판정은 WP-002가 소유한다.
- 검사 선택, 자동 수정 반복, CI 최종 소비 lifecycle은 WP-004가 소유한다.

## Dependencies

- WP-002 — 어떤 변경과 품질 의무를 행동으로 증명할지 확정하려면 고정 정책에 대한 코드 및 리뷰 판정 결과가 먼저 필요하다.

## Adopted Initiative Decisions

- 승인된 checklist의 e-1~e-4는 커버리지 수치만으로 통과시키지 않고 실행 및 변이 증거를 사용한다.
- 테스트의 edge case, 실패, side effect, mock 경계를 사람 리뷰 없이 자동 판정한다.
- 필수 실행 능력이나 증거가 없으면 `INCONCLUSIVE`로 실패한다.

## Decisions Reserved For Matt

- 기본 검사와 심층 검사에서 요구할 실행, 변이, edge case 증명의 범위를 정한다.
- 생성된 일회성 probe와 저장소에 남는 회귀 테스트가 각각 어떤 완료 증거가 되는지 정한다.
- 테스트를 실행할 수 없거나 변이가 적용되지 않는 프로젝트에서 사용자에게 제공할 실패 및 재시도 경험을 정한다.

## Reference Material

- 승인된 상위 Project Map: ../PROJECT-MAP.md

## Matt Start

이 Work Package만을 계획한다. 먼저 Outcome, Included Product Scope, Excluded Sibling Scope를 다시 말하고 사용자가 현재 대화에서 이미 승인한 frame이 정확한지 확인하거나 수정한다. 그다음 확인된 package 범위 안에서 정상 `ask-matt` 흐름을 진행한다. sibling 범위를 흡수해야 한다면 Project Shaper로 shaping delta를 반환한다.
