# WP-004: LLM 자동 운영 및 수정 Loop

Status: ready-for-matt
Parent-Project-Map: ../PROJECT-MAP.md
Work-Package: WP-004
Project-Root: /home/user01/project/iis-skills/adapter/node
Suggested-Work-Slug: node-policy-checker-wp-004-autonomous-loop

## Authority Notice

이 brief는 승인된 프로젝트 분해를 투영하며 첫 Matt 대화의 활성 package 범위를 고정한다. 승인된 package Spec이 아니고 상세 요구사항을 조용히 추가하지 않는다. Matt는 `to-spec` 전에 계약 범위의 공유 이해를 사용자에게 확인받아야 한다.

## Product Context

개별 검사가 존재해도 LLM이 일부를 건너뛰거나 실패 뒤 정책을 바꾸면 비개발 사용자가 신뢰할 수 없으므로, 전체 검사와 수정 반복을 하나의 고정된 자동 lifecycle로 운영해야 한다.

## Package Outcome

LLM은 입장 심사부터 코드 품질 판정과 행동 증명까지 빠뜨리지 않고 실행하며, 고정된 정책 아래 실패를 자동 수정하고 재검사하여 CI가 신뢰할 수 있는 최종 결과를 남긴다.

## Included Product Scope

- LLM이 WP-001의 입장 심사, WP-002의 코드 및 리뷰 판정, WP-003의 행동 증명을 선택된 검사 깊이에 맞춰 자동 호출한다.
- 수정 반복 동안 최초 정책, baseline, Write Permit과 검사 의무를 유지하고 재생성으로 실패를 회피하지 못하게 한다.
- `FAIL`과 `INCONCLUSIVE`의 근거를 LLM 수정 입력으로 제공하고 동일한 의무에 대해 재검사한다.
- 무한 수정, 동일 실패 반복, 환경 미충족에 대한 종료 결과를 사용자에게 명확히 제공한다.
- CI는 정확한 source와 고정 정책에 묶인 최종 `PASS`만 성공으로 소비하고, 사용자는 비기술적 최종 요약을 받는다.

## Excluded Sibling Scope

- 개별 설계, 코드, 리뷰 및 테스트 검사 규칙의 제품 의미는 WP-001, WP-002, WP-003이 각각 소유한다.
- LLM이 수행할 실제 제품 기능 구현과 배포는 이 이니셔티브의 범위 밖이다.
- Node/TypeScript 이외 언어의 자동 운영은 후속 이니셔티브다.

## Dependencies

- WP-003 — 전체 checklist를 자동 운영하려면 실행 및 변이 기반 행동 증명까지 독립적으로 이용 가능해야 한다.

## Adopted Initiative Decisions

- 최종 사용자는 코딩 지식 없이 LLM이 검사와 수정 반복을 빠뜨리지 않게 운영할 수 있어야 한다.
- 최초 정책과 baseline은 수정 반복 동안 고정되며 LLM은 재생성으로 실패를 회피할 수 없다.
- CI와 완료 판단은 정확한 source에 묶인 최종 `PASS`만 성공으로 소비한다.

## Decisions Reserved For Matt

- 사용자가 요청할 수 있는 검사 깊이와 자동 수정 반복의 기본 및 최대 범위를 정한다.
- 동일 실패, 새로운 실패, 환경 실패, 정책 충돌에서 각각 언제 중단하고 무엇을 사용자에게 보여줄지 정한다.
- CI와 대화형 LLM 사용에서 최종 결과의 신선도와 재사용 가능성을 어떻게 경험하게 할지 정한다.

## Reference Material

- 승인된 상위 Project Map: ../PROJECT-MAP.md

## Matt Start

이 Work Package만을 계획한다. 먼저 Outcome, Included Product Scope, Excluded Sibling Scope를 다시 말하고 사용자가 현재 대화에서 이미 승인한 frame이 정확한지 확인하거나 수정한다. 그다음 확인된 package 범위 안에서 정상 `ask-matt` 흐름을 진행한다. sibling 범위를 흡수해야 한다면 Project Shaper로 shaping delta를 반환한다.
