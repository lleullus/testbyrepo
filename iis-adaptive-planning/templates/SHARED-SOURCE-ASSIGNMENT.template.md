# Shared source assignment

Use this block in the existing shared context for every delegated role in the same Adaptive invocation, including delegated planning/review roles and children of a preparation lead. Resolve it once from admitted inputs and approved lineage; forward the same block unchanged, not a role-selected subset. This is an assignment template, not a new project artifact, admission tool or host-enforced injection mechanism.

## Common original sources — forward unchanged

- Project Root: <exact absolute path>
- Thesis: <exact recoverable approved source path(s), bound revision/identity and existing binding reference>
- Repository investigation: <exact supplied immutable artifact path and recorded identity | None supplied>
- Transition Baseline: <exact applicable approved BASELINE-NNN.md path, revision and approval/applicability reference | Not activated>

Do not replace an applicable source with None, latest-file discovery, an Outer Main summary or excerpts selected to support the assignment. Preserve existing multi-source Thesis bindings. Do not create a Thesis or investigation merely to fill this block when the original workflow does not require one; record its actual absence and reason under the existing admission rules. Source revision changes require the existing owning authority/re-entry path, then a refreshed common block for affected assignments; do not silently overwrite or carry forward stale identities.

Read the bound originals before accepting the role-specific framing: Thesis purpose, core completion loop and false-success distinctions; investigation facts, assumptions and limits; and, when activated, BASELINE-NNN Goal, applicable Global/Path Invariants, current Block Exit and Insufficient-for-Exit, Safe Continuation and Safe Abort boundaries. Source anchors aid navigation but do not hide the full recoverable originals.

Thesis carries approved meaning. Investigation is evidence, not approval or proof of current runtime state. Transition Baseline retains its explicit activation and authority rules. Access to its original map does not activate future Blocks or turn them into a work queue. Compare the Active Block projection with its source; retain exactly one active execution envelope.

## Role-specific assignment — caller supplies separately

- Role and exact pinned role-document paths: <read and apply before work>
- Current target: <exact Increment/Spec/Ticket/plan/report and current binding as applicable>
- Active Block Envelope: <existing current projection | Not activated>
- Role question and outputs: <existing role contract>
- Current user instructions and selected mode/model: <exact applicable decisions>
- Navigation and known findings: <primary evidence/report locations; explicitly separate suggestions from obligations>
- Permitted actions and effect/cleanup boundaries: <existing authority>
- Terminal and next owner: <existing role return contract>

The common originals do not expand this role's Ticket/Increment or confer external-action authority. A narrower assigned task cannot redefine approved success. If role framing or a projected contract omits required meaning, identify the exact source and mismatch through the role's existing correction/evidence-limit return; do not silently repair product authority, obey a weaker success condition, or implement the whole Thesis. Keep existing Communication requirements and host profiles unchanged.
