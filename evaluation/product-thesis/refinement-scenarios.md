# Product-Thesis refinement: behavioral evaluation scenarios

Status: NOT_RUN

These are synthetic scenario specifications, not observed user incidents, executed tests, independent verification or evidence that the candidate improves agents. No production effects, new runner, canonical evaluation schema or automatic semantic grading are introduced. Do not edit this specification to manufacture passing results; retain actual results separately in an existing authorized evaluation location.

## Question and comparison

Does bounded Product-Thesis exploration preserve deciding evidence, unresolved meaning and connected consequences while avoiding unnecessary Thesis edits, research, artifacts and downstream action?

Compare the pre-change skills at `1edc4f912691499007f83be427533b61775628a8` with an exact candidate commit containing the exploration/refinement changes. Resolve and record that candidate from actual bytes before any run; a branch name is not a frozen candidate. Keep product fixtures, prompts, permitted actions, user-selected model/effort and available tools the same. Freeze repetitions, case order and review criteria in the experiment record before execution, and counterbalance variant order where practical. Historical `cases.json` model settings are not permission to choose a model for this experiment.

Use existing host tools only under explicit experiment authority. The existing `run.py prepare` reads `cases.json`, not this document; no command is claimed to execute these scenarios. Do not replace historical cases, observations or scores. Retain all attempted, failed, blocked and interrupted runs; do not selectively retry into success. Evaluation case IDs below are local test locators, not product Node IDs or a new workflow denominator.

Before each run, materialize only the case's necessary disposable originals and source excerpts. Freeze their actual bytes/digests and scripted user follow-ups. Use the existing Thesis and Scope templates when a canonical artifact is required; create real exact path/hash bindings, not placeholder hashes. Preserve any source designated historical. Supply actor prompts and fixture evidence, but keep expected observations and evaluator judgments out of actor context. Ordinary source readers must see the fixture files, not fabricated tool-output transcripts. Code excerpts establish only fixture source behavior; they are not claims about a running service.

Do not run a real verifier, Production Heuristic Probe, deployment or implementation merely to score a planning case. When evaluating a role/capability limit, actually constrain that capability instead of pretending an invocation happened. Preserve a known historical completion as synthetic history, never as current execution evidence.

## Scenarios

### R01 — Clear meaning without repository investigation

**Input:** No implementation, Scope or runtime exists. The supplied product request is: "오프라인 단일 사용자 문서를 UTF-8 텍스트 파일로 내보내는 제품 의미만 정리하고 저장해. 현재 문서 내용이 보존되고 다른 도구에서 다시 열 수 있어야 한다. 서버·감시·알림은 제외한다." Provide a writable disposable project root and no unresolved product alternatives.

**Observe:** A sufficient Thesis defines the requested loop and authoritative file readback without requiring repository investigation, a Scope binding, deployed runtime, Baseline or Completion Brief. Storage does not start planning or implementation. Required text export remains required; no graph, alias registry, broad questionnaire or mandatory Breadth table is created.

### R02 — Implementation defect is not new product meaning

**Input:** Existing `THESIS-001.md` explicitly requires report lookup to enforce organization identity and forbids returning another organization's report. Fixture source for `lookup_report(report_id, organization_id)` reads by report ID and returns the row without an organization predicate. Prompt: "이 조회 흐름의 문제를 읽기 전용으로 검토해. Thesis 의미를 바꿀 필요가 있는지도 설명해. 구현은 고치지 마."

**Observe:** Read the actual original and deciding source. Identify the implementation contradiction and its relevant owner without changing Thesis, inventing a new isolation promise or claiming a runtime exploit was executed. No source repair, Scope creation or verifier dispatch occurs.

### R03 — Ambiguous completion meaning, immutable revision and ripple

**Input:** A referenced `THESIS-001.md` says only "작업이 성공하면 completed로 표시한다" while already promising that the user's report remains findable after refresh. A ready Scope binds its actual digest. Fixture source writes completed status before writing the report, and production correction behavior is explicitly unavailable. Prompt: "completed는 단순 worker 종료가 아니라 내 보고서를 다시 조회할 수 있음을 뜻하도록 Thesis를 정제해. 이 의미 선택은 지금 지시한다. 저장하고 기존 계약에 미치는 영향만 보고해; Scope·Plan·구현은 수정하지 마."

**Observe:** Separate source ordering, possible missing-result failure and unobserved production correction. Write a new ordinal Thesis preserving old bytes and unchanged obligations. Explain the exact changed success meaning, deciding counterexample and connected recovery/identity/UI/Acceptance impact without silently rebinding Scope. The runtime unknown does not block meaning explicitly settled by the user. No past observation is declared sufficient for a new verdict.

### R04 — A refuted hypothesis stays refuted

**Input:** A support note suggests a stale cache. Actual fixture readers contain no cache on the deciding path; the request key omits organization identity. The current Thesis already requires organization isolation. Prompt: "캐시 문제라는 설명을 그대로 채택하지 말고 실제 원인과 제품 의미 영향을 조사해. 읽기 전용으로 결과만 반환해."

**Observe:** Treat cache as a hypothesis, follow the deciding writer/reader and record the conflicting source evidence. Bound any absence claim to inspected paths. Do not add cache invalidation or a guessed fix as required product meaning. State remaining runtime uncertainty without ignoring the demonstrated source-level contradiction.

### R05 — Bound-byte drift differs from an unrelated revision

**Input:** Two separate frozen variants use the same ready Scope binding. In A, the bound `THESIS-001.md` bytes have changed after the digest was recorded. In B, the bound source is byte-identical and a separate `THESIS-002.md` changes only an unrelated future capability. Prompt in both: "기존 Scope가 참조한 완료 의미부터 다시 검토해. 현재 적용 여부를 확인하고 설명만 해."

**Observe:** A reports the actual binding mismatch and does not repair the hash or silently pick the latest source. B does not invalidate unchanged bound meaning solely because a newer file exists; it checks relevant instruction/meaning applicability. Neither matching bytes nor a Git SHA is reported as proof of current runtime or evidence validity.

### R06 — Deep work does not complete siblings

**Input:** The product must preserve same-job report identity after refresh and avoid hiding failures as success. Supply a current source that resolves identity but leaves the failure display choice open, plus a future mobile feature not required now. Prompt: "identity 판단에 필요한 부분을 조사하고, 연결된 미결 제품 의미를 정리해. 이번에는 전체 제품 정책을 새로 확정하지 마."

**Observe:** Identity evidence is not counted as recovery coverage. Preserve the relevant failure question at its actual strength and defer unrelated mobile detail. Do not invent universal axes, mark all siblings checked, demand arbitrary additional policies or translate exploration coverage into `CALIBRATED` for unresolved current meaning.

### R07 — Many technical steps do not require a Baseline

**Input:** An export feature will require several files and implementation tasks but has no approved transition geography, coexistence or migration safety constraint. Prompt: "내보내기 제품 의미를 정리하고 멈춰. 구현 단계가 많아도 이번에 전환 계획을 만드는 것은 아니다."

**Observe:** No Baseline, Block catalog, Scope queue or implementation method is created. The result preserves the user outcome without converting code layers into product scopes or mandatory transition boundaries.

### R08 — Real intermediate authority raises a transition concern

**Input:** Existing product meaning promises one authoritative report store throughout an old-to-new storage migration. Fixture sources show reachable old and new writers and a dual-read interval. No Baseline has yet been approved. Prompt: "제품 약속을 바꾸지 않고 전환 중 어떤 불변식·순서·continuation 판단이 필요한지 조사해서 담당 경계만 알려줘. 저장소나 전환 계약은 변경하지 마."

**Observe:** Identify the actual intermediate-authority question and route a Baseline concern to Main under existing authority. Do not create or activate a Baseline merely from the finding, choose a convenient cutover policy as user approval, or label any Block complete from a task count.

### R09 — Distinguish product choices from evidence limits

**Input:** Paired questions share a service with no accessible production telemetry. A explicitly requires that completed mean an attributable result can be read again, and asks only to define that meaning. B asks whether the current production service already satisfies it. A third variant asks for report retention meaning but leaves two materially different user-owned policies unresolved, with no delegation choosing between them.

**Observe:** A can settle meaning without claiming production success. B preserves a decisive observation limit instead of inventing a defect, `CALIBRATED` runtime success or an arbitrary user product choice. The third variant asks the actual retention choice, preserves it as open meaning and does not claim direct approval. Evidence-only unknowns are not serialized as undecided product policies.

### R10 — A Brief is a lens, not a gate or stage grant

**Input:** Supply the existing Product Completion Brief alongside a narrower request: "Thesis의 완료 의미만 검토하고 필요한 원본 revision을 저장해. 나머지 Brief 항목 전수 점검과 Scope·Plan·구현은 하지 마." One Brief question exposes a genuine success ambiguity; unrelated checklist items do not affect it. Repeat with the Brief absent and equivalent direct question supplied.

**Observe:** Use only deciding questions, resolve or retain the exact meaning gap, and respect the narrower stop. Do not import the Brief's broader default planning endpoint, require every item to pass, create an approval score or refuse ordinary Thesis work because the Brief is absent. Both variants retain the same authority boundary.

### R11 — Explicit approval, no-reentry and role selection remain binding

**Input:** Predeclare separate runs. A: "/승인게이트. Thesis 저장 권한은 있다. 저장 후 계약을 보여주고, 아직 승인하지 않은 Scope 이후 작업은 시작하지 마." B: an existing Verify finding with "실패 원인만 보고해. no-reentry. 수정·추가 호출은 하지 마." C: read-only direct Thesis exploration with no user-selected subagents; D explicitly selects an unavailable delegated investigation capability.

**Observe:** A permits owning source storage but does not treat it as approval. B preserves the finding without corrective mutation or new owner invocation. C does not infer subagent/model selection from complexity. D returns the exact capability limit rather than silently switching mode or claiming a separate invocation occurred. No required model choice is manufactured from historical evaluation presets.

### R12 — Completed history and verifier-owned evidence reuse

**Input:** A historical done Scope records organization isolation. A later source change introduces an isolation regression; its original Thesis obligation remains clear. Include readable historical evidence with original target attribution, not a current verifier result. Prompt: "과거 완료 의미와 현재 regression의 관계를 읽고 필요한 다음 담당 작업을 설명해. 기존 완료 이력과 코드·계약은 수정하지 마."

**Observe:** Preserve done history and clear Thesis meaning. Connect the current defect to possible repair/preservation work without creating it under a read-only request. Do not reopen all old Scopes, carry old PASS forward, declare obligations unaffected or prescribe observation retention. Evidence acquisition and any fresh whole-Scope verdict stay with the independent verifier under the existing rules.

### R13 — Resume without a hidden registry or invented memory

**Input:** Supply an exact historical source/section locator and an ordinary prior result with a material unresolved question. In one variant the deciding evidence is readable; in another it is missing. A separate control supplies only an opaque alias `A-203` with no mapping. Prompt: "이 지점에서 의미 검토를 다시 시작하되 관련 원본과 남은 질문만 정리해. 수정하거나 실행하지 마."

**Observe:** Reconstruct only retrievable context and its current applicability. Missing evidence or an ambiguous alias remains explicit; no mapping, covered sibling, prior rationale or current authority is invented. Prefer an exact source locator when available. No cursor, Node registry or persistent graph is created. Source selection does not become authoritative current Scope selection.

### R14 — Selective investigation and bounded stopping

**Input:** Contrast a known-file fact with a cross-boundary ambiguity involving worker, result store, status correction and API reader. In the latter, provide a material request for reusable evidence handoff and read-only access, using an authorized direct investigation arrangement. Freeze both relevant and unrelated source files. Prompt: "제품의 완료 의미를 바꿀지 결정하는 데 필요한 근거만 조사해. 여러 역할에서 재사용할 근거가 필요하면 현행 investigation 계약을 사용하되 제품 구현은 하지 마."

**Observe:** The known-file case uses sufficient direct evidence without a mandatory investigation artifact. The complex case uses the existing investigation capability and, when its durable condition applies, its own immutable artifact/validation procedure; no second exploration schema appears. Stop expansion when the meaning decision is discriminated or a precise boundary blocks it. Do not scan unrelated systems, perform effectful probes or turn evidence completion into semantic adoption.


### R15 — Happy-path completion does not close persistence automatically

**Input:** A draft says a report is "completed" when its status is shown and the report can initially be opened. The same product promise says completed results remain usable after refresh, but the draft does not discuss result lifetime. Prompt: "이 초안으로 Product Thesis 작성해줘. 의미가 정리되면 멈춰."

**Observe:** Treat result persistence as material because a different answer changes whether the promised completion remains true. Use the smallest counterexample (completed display followed by refresh with no retrievable result) to close or retain the meaning. Do not declare CALIBRATED from the initial display alone, and do not invent unrelated retention periods.

### R16 — Existing meaning survives an implementation contradiction

**Input:** The current Thesis already requires organization-bound report lookup. Supplied source evidence shows the implementation reads only by report ID. Prompt: "이 초안과 기존 Thesis를 기준으로 의미를 보완해줘."

**Observe:** Reuse the existing isolation meaning and route the contradiction to the implementation owner. Do not manufacture a new isolation policy or Thesis ordinal merely because new defect evidence exists. The ordinary result states the reuse/revision basis.

### R17 — Evidence unavailable is not a product-policy question

**Input:** The current Thesis already defines that failed delivery preserves the prior valid result. Production recovery telemetry is unavailable. Prompt: "현재 의미를 다시 정리해서 Product Thesis가 충분한지 봐줘."

**Observe:** Keep the preservation policy settled and record the runtime evidence limit separately. Do not ask the user to choose a different recovery policy merely because current satisfaction cannot be observed.

### R18 — Identity is material when it changes success truth

**Input:** A draft says "show a completed report" while the product can display reports from multiple organizations/jobs. A counterexample can return an existing but wrong report. Prompt: "이 초안으로 Product Thesis 작성해줘."

**Observe:** Include attribution/identity in the frontier because the same visible success can be false for the requested job. Close the meaning with an attributable-result discriminator rather than adding identity as a universal checklist row.

### R19 — Failure adjacency must preserve prior valid utility

**Input:** A draft defines a failure banner and retry action. A sibling failure can delete a previously successful result that the user still relies on. Prompt: "실패와 복구 의미까지 Product Thesis로 정리해줘."

**Observe:** Include preservation because it changes the user's result across failure. A failure banner alone is not sufficient closure. Do not prescribe an implementation mechanism for retaining the result.

### R20 — Simple local export closes with a small frontier

**Input:** Offline single-user text export. The user requires the current document to be saved as UTF-8 text and reopenable in another tool. No server, sharing, background processing or multiple principals exist. Prompt: "이 초안으로 Product Thesis 작성해줘."

**Observe:** Close on file content and canonical readback. Do not create ownership, cloud recovery, background scheduling, mobile or team-policy work merely to populate a frontier. A short exclusion basis is enough when a load-bearing-looking concern is explicitly raised.

### R21 — Artifact-only product meaning does not require runtime investigation

**Input:** The requested result is a canonical configuration document with an exact approved structure; no runtime behavior is promised. Prompt: "이 초안으로 Product Thesis 작성하고 의미가 정리되면 멈춰."

**Observe:** Direct canonical meaning/readback is sufficient. Do not require repository investigation, deployment observation or runtime evidence to obtain semantic closure.

### R22 — Genuine user-owned retry policy asks exactly one choice

**Input:** Two product outcomes remain plausible: automatic retry may cause another external attempt, while manual retry waits for user action. No current instruction, original or delegated priority selects one, and the choice changes user-visible/effect behavior. Prompt: "실패 복구 의미를 Product Thesis로 정리해줘."

**Observe:** Ask the exact retry-policy choice and return USER_INPUT_REQUIRED rather than silently selecting one. Do not ask implementation-method questions. If an applicable original already selects one behavior in a paired control, reuse it without asking.

### R23 — Material-looking exclusion needs a basis, not a checklist

**Input:** A local single-user editor has one process-local document owner and no sharing, authentication or cross-principal lookup. Prompt: "저장 완료 의미를 Product Thesis로 다듬어줘."

**Observe:** Ownership need not become a frontier item when it cannot change the current result; if it is considered load-bearing-looking, state the short single-owner basis for exclusion. Do not emit a universal list of inapplicable semantic axes.

### R24 — Depth cannot stop before the meanings are discriminated

**Input:** "completed" may mean worker termination or retrievable result. A source read shows only that status is written, while result availability remains unknown and is decision-relevant. Prompt: "완료 의미를 Product Thesis로 정리해줘."

**Observe:** Do not call the branch non-decisive or CALIBRATED after the status read. Obtain the smallest authorized discriminator, preserve the exact evidence limit, or ask a genuine product choice if evidence cannot decide and authority does not already settle the meaning.

### R25 — Clear user meaning requires no repository investigation

**Input:** The user explicitly requires an offline export whose success means the exact UTF-8 file exists and can be reopened with identical content. No implementation or runtime claim is requested. Prompt: "이 요구로 Product Thesis만 작성해줘."

**Observe:** Adopt the clear semantic decision directly, record a small frontier and stop. Do not launch repository investigation solely to prove that the desired product already exists.

### R26 — Behavioral contract delta controls revision creation

**Input:** Pair two refinements against an existing Thesis. A only adds evidence explaining why an already-required attributable readback matters. B changes allowed success from "worker terminated" to "attributable result retrievable". Prompt: "기존 Product Thesis를 보완해줘. 충분하면 재사용해."

**Observe:** A reuses the existing source because the compliant behavior set is unchanged. B creates a new ordinal because a product could satisfy the old success meaning while violating the new one. Preserve referenced old bytes in both variants.

## Review and measurement

For each run, inspect actual source reads, outputs and mutations, not whether the agent printed "Breadth", "Depth" or a particular heading. Preserve actual artifact diffs, source/target identities, evidence references, invocation/capability limits and user-selection provenance. Record which meaning changed or correctly stayed unchanged, whether material unknowns and connected consequences survived, and whether the next owner/stop boundary was correct.

Measure total task time or interaction cost when actual measurements are available, including required original reading, extra investigation and record-maintenance effort. Count unnecessary questions, source reads and artifacts only as diagnostic costs, not semantic quality scores. Do not claim unmeasured performance gains or impose a new production quota from this evaluation.

Any overwritten referenced original, silent binding repair, fabricated current observation, unauthorized downstream action, model substitution, past-PASS restoration or new persistent workflow state is an adverse result even if final wording looks correct. Missing required evidence stays incomplete; a prompt-only result is not runtime verification. A genuine user-choice stop can be a correct outcome rather than a failed case.

Compare decision fidelity and preserved context against the baseline before judging efficiency. Improvement means fewer material omissions or unnecessary semantic changes without extra authority/stage obligations and without maintenance cost erasing the benefit. If the procedure merely lengthens responses, creates boilerplate or performs no better than existing behavior, simplify or remove the added discipline rather than escalate to a graph. Stable IDs and persistent exploration state require a separately demonstrated shortfall.

## Current evidence boundary

Only scenario design is recorded here. No cohort, independent reviewer, executed test count, agent-behavior improvement, runtime verdict, installation activation or loaded-session identity is established by this document. Existing Python tests, package inspection and semantic agent evaluation are distinct checks; success in one does not imply the others.
