---
name: batch-grill-me
description: Use only when the user positively and explicitly requests batch-grill-me, Batch Grill Me, or 배치 그릴 미 for the current exact Matt planning unit; adapt that interaction mode into IIS without owning a lifecycle phase.
disable-model-invocation: true
---

# Batch Grill Me Adapter

This is an explicit-only IIS interaction adapter, not a lifecycle leaf, planning
authority, fallback, or independent interview owner.

Activate only when the user's current action request positively names
`batch-grill-me`, `Batch Grill Me`, or `배치 그릴 미` as the desired mode. A
definition request, quotation, prior use, negation, mere mention, or generic
request for many/all questions does not activate it. If this adapter or a
required canonical IIS leaf cannot load, block instead of substituting normal
Grill or another route.

After activation, select the normal IIS lifecycle route. Bind
`interaction-mode: batch-grill-me` only to the current exact Matt planning unit
and pass it to `ask-matt`, or after a Project Shaper brief preflight to the
package's Matt flow. `grilling` remains the sole owner of the dependency graph,
frontier rounds, recommendation, gate, approval, and delta policy.

Carry this mode through product-decision bundles identified during the
Matt-performed Behavior Design phase. Clear it when the user cancels it, the final integrated
shared understanding is confirmed, or work changes to another planning unit or
Work Package.

Do not create a Batch artifact, finalize a shared understanding, perform
Behavior Design itself, write a Spec/Ticket, dispatch an invented
research role, or start implementation. `UPSTREAM.md` preserves the external
source; it is not executable authority.
