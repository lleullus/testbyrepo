# UI prototype 검토 예시

이 문서는 특정 UI 질문을 검토한 비권위 prototype 메모다. 가상의 화면에서 정보를 먼저 보여 주고 그 다음 하나의 주요 행동을 제시하는 순서를 비교했다.

사용자는 이 순서를 채택하기로 했다. 그러나 이 메모의 결과 자체는 승인된 UI/UX 참조나 구현 권위가 아니다. 채택된 결정은 별도 `UI-UX.md`에 승인 상태로 기록되며, 그 문서와 승인된 `SPEC.md`만 이후 범위의 근거가 된다.
