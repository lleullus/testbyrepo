# Lumin Repo Lens OMP plugin package

The repository root is the installable OMP plugin. It declares
`omp.extensions` in `package.json`, exposes commands from `commands/`, and
ships skills plus the complete evidence engine under `skills/`.

Run `npm run verify`, `npm test`, and `npm run smoke` before publishing.
